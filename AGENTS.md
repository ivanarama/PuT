# OneBase — руководство для ИИ-разработчика

Этот файл сгенерирован командой `onebase ai-guide` из самой платформы.
OneBase — 1С-подобная платформа: конфигурация описывает прикладные объекты
(метаданные YAML) и логику (DSL `.os`), платформа их исполняет.

## Структура репозитория конфигурации

```
config/app.yaml         настройки приложения (имя, язык, БД)
config/home_page.yaml   главная страница
catalogs/*.yaml         справочники
documents/*.yaml        документы
registers/*.yaml        регистры накопления
inforegs/*.yaml         регистры сведений
enums/*.yaml            перечисления
constants/*.yaml        константы
reports/*.yaml          отчёты (+ запросы)
processors/*.yaml       обработки (мастера с параметрами)
subsystems/*.yaml       подсистемы (структура интерфейса/прав)
journals/*.yaml         журналы документов
widgets/*.yaml          виджеты главной страницы
pages/*.yaml            страницы (произвольные представления на DSL, из меню)
roles/*.yaml            роли (RBAC)
forms/                  управляемые формы (опционально)
src/*.os                модули DSL (логика объектов, обработок, отчётов)
```

Имена файлов `.os` (в нижнем регистре) соответствуют объектам:
`<объект>.posting.os` — проведение документа, `<обработка>.proc.os` —
логика обработки (процедура `Выполнить()`), `<отчёт>.rep.os` — отчёт,
`<объект>.manager.os` — модуль менеджера, `<страница>.page.os` —
обработчик страницы (процедура `ПриФормировании(Страница, Параметры)`, план 66).

## Управляемые формы (managed) — типовые грабли

Форма = forms/<сущность-строчными>/<форма>.form.yaml (разметка) + одноимённый
.form.os (обработчики). `onebase forms validate --src <файл>` ловит структуру,
но открой форму в браузере — часть проявляется только там:

- Команды из секции commands: рисуются автоматической командной панелью — объявил
  команду, она видна и кликается. Элемент kind: Кнопка нужен, только чтобы поставить
  команду в конкретное место формы (тогда в автопанель она не дублируется). Команда
  без action не отрисуется нигде (валидатор: W014).
- Реквизит формы (attributes с save: false) типа CatalogRef.X/DocumentRef.X даёт
  полноценный выбор из справочника. А вот перечисление (enum:/EnumRef.), план счетов
  и AnyRef у реквизита формы выбора НЕ дают — для них бери поле объекта
  (data_path: Объект.X) (валидатор: W015).
- Булево-по-смыслу ПОЛЕ СУЩНОСТИ объявляй type: bool (рендерится «Да/Нет»), не
  type: number — иначе оператор вводит 0/1 вручную. Для булева реквизита формы
  используй элемент kind: Флажок.

## События управляемых форм

Событие объявляется блоком `events:` в `.form.yaml` — **ключ это имя СОБЫТИЯ,
значение это имя ПРОЦЕДУРЫ** в одноимённом `.form.os`. Строка
`ПриОткрытии: ПриОткрытииФормы` читается как «событие ПриОткрытии обрабатывает
процедура ПриОткрытииФормы», а не как два разных события. Блок `events:` есть и у
формы целиком, и у любого элемента.

Какой элемент какие события отправляет из браузера (сервер проверяет пару
fail-closed и на неразрешённой отвечает «элемент формы X не отправляет событие Y»):

| Элемент | События |
|---------|---------|
| форма целиком (`events:` в корне) | `ПриОткрытии` |
| `Кнопка`, команда автопанели | `Нажатие`, `Выбор` |
| `ПолеВвода`, `ПолеКода`, `Флажок`, `ПолеДаты`, `Переключатель` | `ПриИзменении`, `Выбор` |
| `ПолеСписка` | `ПриИзменении`, `НачалоВыбора`, `Выбор` |
| `ТабличнаяЧасть` | `ПриИзменении`, `ПриИзмененииСтроки`, `ПриДобавленииСтроки`, `ПослеДобавленияСтроки`, `ПриУдаленииСтроки`, `ПриАктивизацииСтроки`, `Выбор` |
| `Колонка` (ребёнок ТЧ) | `ПриИзменении` — только правка своей ячейки |

Отдельно стоят **серверные события формы**: `ПриЧтенииНаСервере` (исполняется до
формирования HTML; `ВызватьИсключение` в нём даёт 403, и данные не раскрываются),
`ПередЗаписью`, `ПриЗаписи`, `ПослеЗаписи`. Объявляются там же, на уровне формы,
но браузером не вызываются — их запускает сервер на чтении и записи.

**Когда событие фактически стреляет:**

- Поле шапки — по DOM-событию `change` (уход с поля, выбор в списке), а не на
  каждый введённый символ.
- Ячейка табличной части — только после **фиксации** правки, и с debounce 250 мс:
  быстрые правки коалесцируются в один запрос. Одна правка запускает цепочку
  строго последовательно, следующий запрос уходит после ответа предыдущего:
  `ПриИзменении` колонки → `ПриИзменении` таблицы → `ПриИзмененииСтроки`.
  Обработчик колонки обработчик таблицы не отменяет.
- В ячейке-ссылке с открытым выпадающим списком **первый Enter подставляет
  значение и оставляет курсор в ячейке** (ввод по строке, как в 1С); правку
  коммитит только второй Enter или Tab, и до этого события нет вовсе. Типовой
  ложный вывод «обработчик не работает» — обычно про это.
- `ПриАктивизацииСтроки` — на смену СТРОКИ; переход по колонкам внутри строки
  активизацией не считается. Тоже debounce 250 мс, поэтому прокрутка через
  полсотни строк — одно событие на конечной, а не полсотни запросов.
- `ПриДобавленииСтроки`, затем `ПослеДобавленияСтроки` — последовательно, после
  добавления строки (Insert, кнопка «+ Добавить строку», копирование строки F9).
- `ПриУдаленииСтроки` — после удаления (Delete, кнопка).
- `ПриОткрытии` — на КЛИЕНТЕ, после отрисовки страницы: данные к этому моменту
  уже отданы браузеру. Проверка ДО выдачи данных — это `ПриЧтенииНаСервере`.

Подписка ставится только при объявленном обработчике: необъявленное событие
сети не гоняет вовсе.

**Переменные обработчика.** Всегда есть `Объект`/`ЭтотОбъект` и реквизиты формы
голым именем. У событий табличной части дополнительно:

| Переменная | Значение |
|-----------|----------|
| `ИмяТабличнойЧасти` (`ТекущаяТабличнаяЧасть`) | имя табличной части |
| `НомерСтроки` / `ИндексСтроки` | номер с 1 / индекс с 0 |
| `ТекущаяСтрока` | строка ТЧ; читается и пишется |
| `ТекущаяКолонка` (`ИмяКолонки`) / `ИндексКолонки` | правившаяся колонка |
| `ВыделенныеСтроки` | массив выделенных строк |
| `ПодборРезультат` | результат диалога у события `Выбор` (после `ПоказатьПодбор`) |

Приходят они не все и не всегда:

- **`ПриДобавленииСтроки`, `ПослеДобавленияСтроки`, `ПриУдаленииСтроки` приносят
  ТОЛЬКО `ИмяТабличнойЧасти`.** Ни `НомерСтроки`, ни `ТекущаяСтрока` в них не
  передаются, так что заполнить «только что добавленную строку» по номеру в них
  нечем — обработчик видит табличную часть целиком через `Объект.<ТЧ>`. Контекст
  строки и колонки есть у `ПриИзменении`, `ПриИзмененииСтроки`, `ПриАктивизацииСтроки`.
- `ВыделенныеСтроки` приходит, только если выделение реально есть. У ТЧ на
  SlickGrid (режим по умолчанию) модель выделения в поставке не установлена,
  поэтому переменной обычно нет вовсе. Флажки выделения строк рисует режим
  `no_grid: true`, и только когда у табличной части есть команды: без них
  колонки с флажками нет.

**Readonly и права.** Элемент с `readonly: true` — и всё, что лежит внутри
readonly-контейнера, — событий не отправляет: клиент не навешивает обработчик, а
сервер отклоняет такой запрос отдельной проверкой, поэтому подделать его нельзя.
Нет права read/write на сущность — 403; событие на ещё не сохранённой записи
требует write.

## Пустые значения в DSL: незаполненное число это Неопределено

**Незаполненное поле типа number или date приезжает в модуль как `Неопределено`,
а не как 0 или пустая дата.** Форма отдаёт пустую ячейку именно так, в базу
уходит NULL, оттуда же читается обратно. Это отличие от 1С, где у примитивных
реквизитов есть значение по умолчанию и пустого значения у числа не бывает.

Чтобы идиома 1С `Если Стр.Цена = 0` продолжала работать, **операторы сравнения
считают такое `Неопределено` нулём, когда вторая сторона — число:**

```
Пусто = Неопределено;      // невведённое число из ТЧ, поля формы или базы
Пусто = 0            // Истина
Пусто <> 0           // Ложь
Пусто > 0            // Ложь
Пусто >= 0           // Истина
Пусто < 5            // Истина
Пусто + 1            // 1   (арифметика считала пустое нулём всегда)

Пусто = 5            // Ложь — правило одностороннее, подставляется НОЛЬ
Пусто = ""           // Ложь — вторая сторона не число, сравнение как раньше
Пусто = Неопределено // Истина
0     = Неопределено // Истина ТОЖЕ — подстановка симметрична, см. ниже
```

**Правило живёт только на пути операторов** (`=`, `<>`, `<`, `>`, `<=`, `>=` и
утверждения `Утверждать.Равно/НеРавно`). Поиск и сортировка коллекций и ключи
`Соответствие` его не видят — и это единственный способ отличить «пусто» от
«ноль»:

```
ТЗ.НайтиСтроки(Новый Структура("Цена", 0))            // только настоящие нули
ТЗ.НайтиСтроки(Новый Структура("Цена", Неопределено)) // только незаполненные
Массив.Найти(0)          // элемент Неопределено НЕ находит
Соответствие.Получить(0) // запись с пустым ключом НЕ находит
```

**Отличить введённый ноль от невведённого значения сравнением нельзя вообще** —
подстановка симметрична, поэтому и `Х = 0`, и `Х = Неопределено` истинны для
обоих. Годятся только `ТипЗнч` и показанный выше поиск по коллекции:

```
Если ТипЗнч(Стр.Цена) = Тип("Неопределено") Тогда   // цену не вводили
Если ТипЗнч(Стр.Цена) = Тип("Число") Тогда          // ввели, возможно ноль
```

Та же ловушка у поиска: признак «не найдено» у `Найти`/`Получить` — это
`Неопределено`, а `Неопределено = 0` теперь Истина, поэтому
`Если Массив.Найти(Х) = 0` не отличает «не найдено» от «найдено первым
элементом». Проверяй `ТипЗнч`.

`ЗначениеЗаполнено(Х)` отвечает Ложь и для `Неопределено`, и для нуля, и для
пустой строки, и для `Ложь`. Обратная ей `Пустая(Х)` на тех же значениях
отвечает Истина: `Пустая(0)` — Истина, а не Ложь.

Ноль от незаполненного не отличает ни та, ни другая: это проверка «заполнено
ли», а не «пусто ли конкретно». Для различения — `ТипЗнч`.

Даты подстановкой нуля НЕ покрыты (правило числовое): незаполненная дата —
`Неопределено`, и проверять её надо `ЗначениеЗаполнено(Стр.Дата)`.

## Рабочий цикл (всё headless, текст/JSON)

Все команды — через бинарь `onebase`. Если оболочка отвечает `onebase: command not found`,
он не установлен в PATH. Установка (нужен Go): `go install github.com/ivantit66/onebase/cmd/onebase@latest`
и убедись, что каталог `go env GOPATH`/bin есть в PATH. Из исходников:
`go build -o <dir-в-PATH>/onebase ./cmd/onebase`. Проверка: `onebase --help`.
Внутри проекта флаг `--project` можно опускать — берётся текущий каталог.

| Команда | Назначение |
|---------|-----------|
| `onebase check --project <dir>` | Валидация: синтаксис .os, неизвестные функции, YAML-схема, компиляция и исполнение запросов. Выводит `file:line:col: message`, exit code ≠ 0 при ошибках. Запускай после каждой правки. |
| `onebase describe --project <dir> [--compact|--full]` | Вся структура конфигурации + список builtins в JSON (`--full`) или компактный prompt-контекст (`--compact`). |
| `onebase schema [kind]` | JSON Schema для YAML-метаданных: автокомплит/валидация редакторов, ИИ и MCP-клиентов. |
| `onebase fmt --project <dir> --check` | Проверка canonical YAML-формата; без `--check` форматирует файлы на месте. |
| `onebase examples <kind>` | Канонические YAML/DSL-примеры для объектов, запросов и проведения. |
| `onebase query --project <dir> --sqlite <db> '<запрос>' --params '{...}'` | Read-only выполнение запроса OneBase headless, JSON/табличный вывод, `--limit`. |
| `onebase eval --project <dir> '<expr>'` | Выполнение выражения/фрагмента DSL в RestrictedProfile; сеть/файлы/exec/ИИ закрыты. |
| `onebase widget explain --project <dir> <Имя> --json` | Объяснить виджет: запрос, SQL, sources, mapping, sample rows (`--sample N`). |
| `onebase report explain --project <dir> <Имя> --json` | Объяснить отчёт: запрос, SQL, sources, компоновка, sample rows (`--sample N`). |
| `onebase impact --project <dir> --object X [--field Y]` | Статический анализ мест, затронутых удалением/переименованием объекта, поля или процедуры. |
| `onebase refactor rename-field --project <dir> --object X --from A --to B` | Patch preview безопасного переименования поля; запись только с `--write` и обязательным `check`. |
| `onebase refactor rename-object --project <dir> --from A --to B` | Patch preview переименования объекта, включая файл YAML; данные требуют отдельной миграции. |
| `onebase config versions --id <base>` | Список snapshots `_config_versions` для rollback после AI apply/deploy. |
| `onebase config rollback --id <base> <version>` | Откат database-backed конфигурации к snapshot; создаёт новую версию отката. |
| `onebase mcp --project <dir>` | MCP stdio server для внешних AI-клиентов. Read-only по умолчанию; mutating tools включаются точечно (`--allow-refactor-write`, `--allow-config-rollback`, `--allow-fmt-write`, `--allow-procrun`) или общим `--allow-write`. |
| `onebase procrun --project <dir> --proc <Имя> --set К=З --file П=путь` | Запуск обработки офлайн, печать `Сообщить()`. Отладка прикладной логики. |
| `onebase run --project <dir> --sqlite <файл> --port N` | Поднять сервер (UI + REST). |
| `onebase dev --project <dir> --sqlite <файл> --open` | То же для разработки: правки `.yaml`/`.os` подхватываются на лету, открытая страница обновляется сама, `--open` открывает браузер по готовности. |

## Проверка результата (доказательства)

**Зелёный `onebase check` ≠ «работает».** `check` ловит синтаксис,
неизвестные функции и компиляцию/исполнение запросов (`no such column/table`),
но не бизнес-поведение. Доказывай под тип изменения:

- **Проведение, движения, остатки:** через `onebase procrun` создай документ,
  проведи, прочитай движения/остатки `Запросом`, напечатай `Сообщить()` —
  сверь числа. Регистры проверяй и напрямую через `onebase query`.
- **Запрос, агрегаты:** `onebase query … --params '{…}'` на реальных данных,
  сверь с ожиданием.
- **Отчёт, виджет:** `onebase report explain` / `onebase widget explain --json`
  — запрос, SQL, sample rows.
- **UI, формы, дашборды:** headless CLI отрисовку не покажет — либо подними
  `onebase run` (или `onebase dev --open`, где правки видны без
  перезапуска) и проверь браузером, либо честно напиши «UI не проверен headless».
- **Роли, RLS, видимость:** `ИмяПользователя()` пуст в `procrun`, RLS по
  пользователю headless не доказать — нужен сессионный прогон под ролью через
  `onebase run`.

Отчёт о сделанном держи коротким и проверяемым: строка Tool trace (команда → итог),
что создано, какие проверки прошли — не только `check`, но и сверенные числа
`procrun`/`query`, и явно проверен ли UI — и как открыть результат.

## Язык DSL

1С-подобный, ключевые слова и идентификаторы на русском, регистронезависимый.
Строки — UTF-8. Встроенные функции (по группам):

**Email:**

- `ОтправитьПисьмо(Кому, Тема, Текст)` — Отправляет письмо по настроенному SMTP. Требует секцию email: в конфигурации; иначе вызов завершается ошибкой.

**HTTP:**

- `HTTPАдресРазрешён(URL, РазрешённыйИсточник)` — Проверяет схему, точное совпадение домена и порта без сетевого запроса. Используйте как быстрый фильтр; окончательную DNS/IP-проверку выполняет HTTPПолучитьБезопасно.
- `HTTPОтправить(URL, Тело)` — Выполняет POST-запрос по URL с указанным телом (application/json) и возвращает объект HTTPОтвет.
- `HTTPПолучить(URL)` — Выполняет GET-запрос по URL и возвращает объект HTTPОтвет (таймаут 30 с).
- `HTTPПолучитьБезопасно(URL, РазрешённыйИсточник)` — Выполняет GET только к разрешённому публичному источнику: проверяет DNS/IP в момент подключения, фиксирует проверенный IP и повторяет границу на каждом перенаправлении. Несовпадающий URL возвращает Неопределено; private/special-use IP завершает вызов ошибкой.

**HTTP-сервисы:**

- `ОтветJSON(КодСостояния, Значение)` — Строит ответ HTTP-сервиса с JSON-телом (Content-Type: application/json). Используется в обработчиках эндпоинтов /hs/*.
- `ОтветТекст(КодСостояния, Текст, ТипСодержимого)` — Строит ответ HTTP-сервиса с текстовым телом. Используется в обработчиках эндпоинтов /hs/*.

**Глобальный контекст:**

- `БлокировкаДанных()` — Возвращает новый объект управляемой блокировки данных, привязанный к менеджеру блокировок сервера.
- `ВебСокет.ИмяШлюза.Отправить(Строка) | .Подключён()` — Глобальный объект исходящих WebSocket-соединений приёмки (шлюз intake с transport: ws, план 120). Отправить(Строка) пишет текстовое сообщение в живое соединение; формат — дело конфигурации, объект сериализуйте заранее (ЗначениеВJSON). Гарантий доставки нет: обрыв или неподнятое соединение — исключение сразу (перехватывается Попытка/Исключение), буфера и повтора нет. Подключён() — Булево. Входящие сообщения сюда не относятся: их принимает обработчик шлюза (процедура Обработать) через приёмку с идемпотентностью и карантином.
- `ВыполнитьКоманду(Команда, Аргументы = Неопределено, Таймаут = 30, РабочийКаталог = "")` — Запускает команду ОС и ждёт завершения. Возвращает Структуру {КодВозврата, СтандартныйВывод, ОшибочныйВывод, Завершилась}. По умолчанию ВЫКЛЮЧЕНО — включается флагом базы exec.enabled (конфигуратор: Меню → «Параметры базы»); запрещено в песочнице (ИИ/marketplace). Команда и аргументы передаются без shell — инъекция невозможна (план 67).
- `Документы.ИмяДокумента` — Глобальный объект доступа к документам: Документы.X.Создать(), .Записать(), .Провести(), .НайтиПоНомеру(...).
- `ЗаписатьСобытиеАудита(Действие, Вид, Объект, Ссылка, ИдентификаторРешения, Автор)` — Транзакционно пишет обязательное governance-событие публикации или отката в журнал аудита.
- `ЗначениеРеквизитаОбъекта(Ссылка, ИмяРеквизита)` — Читает значение реквизита объекта по ссылке отдельным запросом к БД. Ссылочный реквизит возвращается ссылкой; пустая ссылка — nil; неверное имя — ошибка.
- `ЗначенияРеквизитовОбъектов(Ссылки, ТипОбъекта, Реквизиты)` — Пакетно читает реквизиты объектов одним запросом. Возвращает Соответствие: ключ — ссылка, значение — Структура с запрошенными реквизитами; ссылочные реквизиты возвращаются ссылками.
- `ИмяПользователя()` — Возвращает логин текущего пользователя (пустую строку для фоновых заданий).
- `Нумераторы.СледующийНомер("Сущность"[, ДатаИлиПоля[, Область]])` — Глобальный объект автонумерации из DSL. Для numerator.scope передайте структуру полей либо явную Область; scope обязателен и участвует в ключе счётчика. В DSL-транзакции номер откатывается вместе с ней.
- `ПолнотекстовыйПоиск(Текст, Лимит = 20, ПолеОтбора = Неопределено, ЗначениеОтбора = Неопределено, Объекты = Неопределено)` — Глобальный поиск по справочникам и документам: Массив Структур {Объект, Вид, Ссылка, Представление, ПометкаУдаления, Проведён} по убыванию релевантности. Опциональные отбор и список объектов входят в запрос индекса до ранжирования и лимита. Сущности без ПоляОтбора исключаются. Права — пользователя сессии: недоступные объекты и строки в выдачу не попадают. Состав индекса задаёт `fulltext:` в метаданных объекта.
- `ПредопределённыеЗначения.Справочник.Элемент` — Доступ к предопределённым элементам справочников по имени: ПредопределённыеЗначения.X.Элемент.
- `РегистрыНакопления.ИмяРегистра` — Глобальный объект доступа к регистрам накопления: .Остатки(), .Обороты(), .ВыбратьПоРегистратору(Док).
- `РегистрыСведений.ИмяРегистра.СоздатьМенеджерЗаписи() | .СоздатьНаборЗаписей()` — Глобальный объект доступа к регистрам сведений. СоздатьМенеджерЗаписи() отдаёт менеджер записи: значения измерений и ресурсов присваиваются как реквизиты, дальше Записать/Прочитать/Удалить/Выбран. Период обязателен у периодического регистра и запрещён у непериодического. Регистр, подчинённый регистратору (recorder: true), программной записью не принимается — его строки формирует ОбработкаПроведения. СоздатьНаборЗаписей() отдаёт набор для пакетной работы: Отбор (только по измерениям), Прочитать, Очистить, Добавить, Количество, Записать. Записать замещает содержимое ПО ОТБОРУ одной транзакцией; без отбора отклоняется.
- `РегламентныеЗадания.Запустить("ИмяЗадания") | .Прогон(ИдПрогона)` — Запуск регламентного задания по требованию, не дожидаясь расписания (план 123). Запустить(Имя) возвращает идентификатор прогона и сразу отдаёт управление — задание работает в фоне; ожидания завершения нет, сессия однопоточная. Прогон(Ид) возвращает Структуру с полями Задание, Статус, Начало, Конец, ДлительностьМс, Ошибка, Вывод — либо Неопределено, если журнал такого прогона не помнит. Статус: running, success, error, timeout, interrupted. Параллелизма нет: одно задание в один момент выполняется один раз, попытка запустить занятое даёт перехватываемое Попытка/Исключение исключение «уже выполняется» — это штатная гонка с запуском по расписанию. Запуск внутри транзакции запрещён, в том числе из ОбработкаПроведения: запускайте после записи. Прикладной результат задание возвращает через данные (регистр сведений), а не через журнал прогонов; Вывод — это склейка Сообщить() задания, и она может быть большой. Задание, у которого выключено расписание (enabled: false), запускается — это штатный способ держать «задание по требованию». Если ждёте завершения опросом Прогон(Ид), держите собственный предел ожидания: прогон, прерванный жёстким завершением процесса, остаётся в статусе running.
- `Символы.ПС` — Глобальный объект непечатаемых символов, имена как в 1С: ПС (перевод строки), ВК (возврат каретки), Таб, ВТаб, ПФ (перевод формата), НПП (неразрывный пробел). Английские синонимы: LF, CR, Tab, VTab, FF, NBSp.
- `Справочники.ИмяСправочника` — Глобальный объект доступа к справочникам: Справочники.X.НайтиПоКоду(...), .Создать() и т.п.
- `СсылкаНаОбъект(ИмяСущности, Идентификатор) | СсылкаНаОбъект(Ссылка)` — URL карточки объекта /ui/<вид>/<сущность>/<id> для ссылок на странице (план 66). Форма с одним аргументом принимает объект-ссылку (например из НайтиПоКоду). В результатах запроса колонка Ссылка приходит UUID-строкой без типа — укажите сущность явно и псевдоним: ВЫБРАТЬ Ссылка КАК Ид … → СсылкаНаОбъект("Номенклатура", Стр.Ид). Пустой идентификатор — пустая строка.
- `ТекущийПользователь()` — Возвращает объект текущего пользователя с полями ИД, Имя, ПолноеИмя, Админ.
- `ФоновыеЗадания.Поставить("ИмяЗадания"[, Параметры][, Ключ]) | .Задача(Ид) | .Отменить(Ид) | .Глубина([Статус])` — Очередь фоновых заданий с пулом исполнителей (план 130). В отличие от РегламентныеЗадания.Запустить даёт ПАРАЛЛЕЛИЗМ: одно и то же задание исполняется многими исполнителями сразу, каждый со своими параметрами. Поставить(Имя, Параметры, Ключ) возвращает идентификатор задачи; Параметры — Структура или Соответствие со скалярами (строка, число, булево, дата), они перекрывают params задания поимённо. Ключ — идемпотентность: пока задача с этим ключом ждёт или исполняется, повторная постановка не плодит дубль, а возвращает идентификатор живой задачи. Задача(Ид) возвращает Структуру с полями Задание, Статус, Ключ, Попытки, МаксПопыток, Создана, Начало, Конец, ДоступнаС, ДлительностьМс, Исполнитель, Ошибка, Вывод, Параметры — либо Неопределено, если очередь такой задачи не помнит. Статус: pending, running, done, dead (попытки исчерпаны), cancelled. Отменить(Ид) снимает задачу из очереди или просит исполнителя прерваться. Глубина([Статус]) — сколько задач в статусе (по умолчанию pending); полезно продюсеру, чтобы не заваливать очередь новым кругом. Ставить задачу МОЖНО внутри транзакции, в том числе из ОбработкаПроведения: задача станет видимой исполнителю после коммита, а откат снимет её вместе с данными. Доставка at-least-once: задача, чей исполнитель умер жёстко, вернётся в очередь по истечении аренды и будет исполнена ещё раз — обработка обязана быть идемпотентной. На SQLite исполнитель всегда один (там одно соединение на базу); пул на 16–32 требует PostgreSQL.

**Даты:**

- `Год(Дата)` — Возвращает год даты.
- `Дата(Год, Месяц, День[, Час, Минута, Секунда]) | Дата(Строка) | Дата(Число)` — Конструктор даты: из компонент, из строки ("2026-05-11", "11.05.2026") или числа (20260511). Неподдерживаемый или неразбираемый одноаргументный ввод даёт пустую дату. Небезопасные числовые компоненты и результат с годом вне 0000..9999 дают ловимую DSL-ошибку.
- `День(Дата)` — Возвращает день месяца даты (1–31).
- `ДеньГода(Дата)` — Возвращает порядковый номер дня в году (1–366).
- `ДеньНедели(Дата)` — Возвращает день недели (Понедельник = 1, …, Воскресенье = 7).
- `ДобавитьГод(Дата, Количество)` — Возвращает дату, сдвинутую на указанное число лет (может быть отрицательным); дробь усекается к нулю. Переполнение и результат с годом вне 0000–9999 дают ловимую ошибку.
- `ДобавитьДень(Дата, Количество)` — Возвращает дату, сдвинутую на указанное число дней (может быть отрицательным); дробь усекается к нулю. Переполнение и результат с годом вне 0000–9999 дают ловимую ошибку.
- `ДобавитьМесяц(Дата, Количество)` — Возвращает дату, сдвинутую на указанное число месяцев (может быть отрицательным); дробь усекается к нулю. Переполнение и результат с годом вне 0000–9999 дают ловимую ошибку.
- `ДобавитьМинут(Дата, Количество)` — Возвращает дату, сдвинутую на указанное число минут; год вне 0000–9999 даёт ловимую ошибку.
- `ДобавитьСекунд(Дата, Количество)` — Возвращает дату, сдвинутую на указанное число секунд. Эквивалент `Дата + Количество`: сложение даты с числом трактует число как секунды. Год вне 0000–9999 даёт ловимую ошибку.
- `ДобавитьЧас(Дата, Количество)` — Возвращает дату, сдвинутую на указанное число часов; год вне 0000–9999 даёт ловимую ошибку.
- `КонецГода(Дата)` — Возвращает конец года для указанной даты (31 декабря, 23:59:59).
- `КонецДня(Дата)` — Возвращает конец дня (23:59:59) для указанной даты.
- `КонецКвартала(Дата)` — Возвращает конец квартала для указанной даты (последняя секунда).
- `КонецМесяца(Дата)` — Возвращает конец месяца для указанной даты (последняя секунда последнего дня).
- `КонецМинуты(Дата)` — Возвращает конец минуты (59 секунд) для указанной даты.
- `КонецНедели(Дата)` — Возвращает конец недели (воскресенье, 23:59:59) для указанной даты.
- `КонецЧаса(Дата)` — Возвращает конец часа (59 минут 59 секунд) для указанной даты.
- `Месяц(Дата)` — Возвращает номер месяца даты (1–12).
- `Минута(Дата)` — Возвращает минуты даты (0–59).
- `НачалоГода(Дата)` — Возвращает начало года для указанной даты (1 января, 00:00:00).
- `НачалоДня(Дата)` — Возвращает начало дня (00:00:00) для указанной даты.
- `НачалоКвартала(Дата)` — Возвращает начало квартала для указанной даты.
- `НачалоМесяца(Дата)` — Возвращает начало месяца для указанной даты (1-е число, 00:00:00).
- `НачалоМинуты(Дата)` — Возвращает начало минуты (секунды обнулены) для указанной даты.
- `НачалоНедели(Дата)` — Возвращает начало недели (понедельник, 00:00:00) для указанной даты.
- `НачалоЧаса(Дата)` — Возвращает начало часа (минуты и секунды обнулены) для указанной даты.
- `НеделяГода(Дата)` — Возвращает номер недели в году (неделя 1 начинается 1 января, границы по понедельникам).
- `РазностьДат(Дата1, Дата2, Единица)` — Возвращает разность Дата2 − Дата1 в указанных единицах. По умолчанию (неизвестная единица) — в днях.
- `Секунда(Дата)` — Возвращает секунды даты (0–59).
- `ТекущаяДата()` — Возвращает текущую дату с обнулённым временем (начало дня).
- `ТекущаяДатаВремя()` — Возвращает текущие дату и время.
- `Час(Дата)` — Возвращает час даты (0–23).

**Значения и тип:**

- `Вычислить(Выражение)` — Разбирает строку как выражение DSL и вычисляет его в текущем контексте: видит локальные переменные и процедуры своего модуля. Подставляйте значения через переменные контекста, а не конкатенацией пользовательского текста. Исполняет только выражение, не операторы.
- `ЗначениеЗаполнено(Значение)` — Истина, если значение не пустое (не nil, не "", не 0, не Ложь, не пустая коллекция/ссылка).
- `Пустая(Значение)` — Истина, если значение пустое: nil, пустая строка, 0, Ложь, пустая коллекция или пустая ссылка.
- `ПустаяСсылка(Значение)` — Узкий предикат именно для ссылок: истина для пустой ссылки. В отличие от Пустая, 0/Ложь/пустая коллекция пустой ссылкой не считаются.
- `Тип(ИмяТипа)` — Возвращает нормализованное имя типа-маркер для сравнения с результатом ТипЗнч(). Регистр префикса значения не имеет, а имя объекта конфигурации пишется как в самой конфигурации: метаданных у Тип() нет, выправить регистр нечем, а сравнение строк регистрозависимо. Написание объекта из 1С ("ДокументОбъект.X", "СправочникОбъект.X") принимается синонимом к "Документ.X"/"Справочник.X" — перенесённый модуль сравнивается с тем же маркером. Незнакомый префикс возвращается как есть.
- `ТипЗнч(Значение)` — Возвращает имя типа значения ("Число", "Строка", "Дата", "Массив"…). Объект конфигурации называется квалифицированно — "Документ.РеализацияТоваров", "Справочник.Номенклатура"; ссылка на него — "ДокументСсылка.X", "СправочникСсылка.X". Объект и ссылка — РАЗНЫЕ типы, как в 1С: сравнение объекта с "ДокументСсылка.X" всегда Ложь. Хук ОбработкаЗаполнения получает основание ОБЪЕКТОМ (у него доступны реквизиты и табличные части), поэтому источник распознают по "Документ.X" либо берут ссылку псевдо-реквизитом ДанныеЗаполнения.Ссылка.
- `Формат(Значение, СтрокаФормата)` — Форматирует число (ЧДЦ — знаков после запятой, ЧРГ — разделитель групп) или дату (ДФ — шаблон) в строку.

**ИИ-помощник:**

- `ЗапросИИ(Промпт[, Параметры])` — Отправляет текстовый запрос ИИ-помощнику и возвращает ответ. Требует настроенного помощника.
- `ЗапросИИДжейсон(Промпт[, Параметры])` — Как ЗапросИИ, но запрашивает у модели строгий JSON и возвращает его строкой.
- `РаспознатьДокумент(Путь, Промпт)` — Распознаёт документ из файла через vision-запрос ИИ-помощника и возвращает ответ.
- `РаспознатьИзображение(ДанныеBase64, ТипMIME, Промпт)` — Распознаёт изображение из данных в памяти (без записи на диск) через vision-запрос ИИ-помощника.

**Картинки:**

- `СохранитьКартинку(ДанныеBase64, ТипMIME = "", Владелец = Неопределено)` — Сохраняет картинку и возвращает UUID. Для реквизита типа «Картинка» передавайте ссылку-владельца и записывайте UUID в той же транзакции: доступ к /image проверяется по сущности, а при обычном rollback внешнее содержимое удаляется компенсационно. Аварийное завершение процесса или сбой S3-удаления не дают строгой атомарности. Вариант без владельца оставлен для совместимости, доступен любому аутентифицированному пользователю и не удаляется GC. Пустые данные возвращают пустую строку.

**Коллекции и JSON:**

- `XMLЗначение(ИмяТипа, Текст)` — Обратное к XMLСтрока: разбирает XML-представление в значение типа, возвращённого XMLТипЗнч. Для расширения OneBase undefined принимается только пустое представление. Decimal задаётся без e/E и ограничен 10 000 символами и цифрами; дробная часть dateTime содержит не более девяти цифр, смещение — от -14:00 до +14:00.
- `XMLСтрока(Значение)` — XML-представление примитива: конечное число без экспоненты (до 10 000 символов), дата по RFC 3339 с годом 0000..9999, наносекундами и смещением -14:00..+14:00, булево как true/false, Неопределено как пустая строка. Произвольные объекты не приводятся через String().
- `XMLТипЗнч(Значение)` — Имя XSD-типа поддержанного безопасного примитива: string, decimal, boolean или dateTime; для Неопределено возвращается расширение OneBase undefined. Произвольные объекты не приводятся через String(). Нужно, когда вместе со значением передаётся и его тип.
- `ВыгрузитьВExcel(Данные[, Заголовок])` — Формирует файл XLSX из таблицы (первая строка — заголовки) и возвращает его содержимое в Base64.
- `ЗаписатьJSON(Значение)` — Сериализует значение DSL в строку JSON.
- `ЗаписатьXML(Значение[, ИмяКорня])` — Безопасно сериализует значение в XML с проверкой имён и символов. Дерево из ПрочитатьXML сохраняет структуру, но представление может быть канонизировано; произвольные коллекции записываются как вложенные элементы по именам полей, поддерживаются только безопасные XML-примитивы без пользовательского String(). Суммарный текст и готовый документ ограничены 16 МиБ, атрибуты — 100 000 всего и 10 000 на элемент.
- `ЗаполнитьЗначенияСвойств(Приёмник, Источник[, СписокСвойств, ИсключаяСвойства])` — Копирует одноимённые свойства из Источника в Приёмник. Пустой СписокСвойств означает «все свойства».
- `НайтиНедопустимыеСимволыXML(Текст)` — Позиция первого символа, недопустимого в XML 1.0 (нумерация с 1), или 0, если строку можно поместить в документ без потери.
- `ПрочитатьJSON(Текст)` — Разбирает строку JSON в значение DSL (Соответствие, Массив, число, строку, булево).
- `ПрочитатьXML(Текст)` — Строго разбирает XML в дерево: Структура с полями Имя (строка), Атрибуты (Соответствие), Текст (строка) и Элементы (Массив таких же структур). Пространства имён сохраняются лексически: в Имя попадает квалифицированное имя «префикс:локальное», объявления xmlns остаются обычными атрибутами. Объявление XML в начале документа допускается (только version="1.0") и при записи не восстанавливается. Документ в однобайтовой кодировке (windows-1251/cp1251, windows-1252, cp866/ibm866, koi8-r, iso-8859-1/5) перекодируется в UTF-8 по объявлению; остальные кодировки отклоняются. DOCTYPE, комментарии, инструкции обработки и смешанное содержимое не поддерживаются; вне корня допустимы только буквальные XML-пробелы и начальный UTF-8 BOM. Пределы: 16 МиБ документа, 100 000 элементов, 100 000 атрибутов всего и 10 000 на элемент.

**Оборудование:**

- `ПодключитьОборудование(Драйвер, Параметры)` — Подключает торговое оборудование и возвращает объект-устройство. Доступные методы зависят от типа драйвера: НапечататьЧек, ЗарегистрироватьЧек (→ Структура с ФН/ФД/ФП/QR), Показать, Очистить, ПолучитьВес, ОплатитьКартой, ОткрытьЯщик, Отрезать, Отключить. OneBase — однопользовательское десктоп-приложение: сервер на машине кассира подключается к локальным устройствам напрямую. Соединение закрывайте методом Отключить().

**Прочее:**

- `Приостановить(Секунды)` — Блокирующая выдержка времени — интервал между повторами при 429/503, выдержка Retry-After. Числовые строки не принимаются. Предел одного вызова 300 с; в песочнице ожидание прерывается по остатку общего MaxWallClock запуска. Под замороженными часами (Часы.Установить в тест-профиле) время не тратится, а сдвигается вперёд, вызов записывается в Мок.Паузы — backoff проверяется headless и мгновенно.

**Реальное время:**

- `ОтправитьУведомление(Кому, Событие, Данные)` — Публикует событие в real-time-шину «сервер → браузер» (SSE, план 74): доставляется подписанному пользователю по логину, роли ("роль:Имя") или всем ("*"). На странице ловится window-событием 'onebase:<Событие>'; событие "уведомление"/"notify" со строкой показывается тостом автоматически.
- `ПоказатьОповещениеПользователя(Кому, Заголовок, Текст, Ссылка, Важность)` — Аналог «УведомленияКлиента» 1С (план 87, ступень B): богатый тост у пользователя. Тонкая обёртка над ОтправитьУведомление(Кому, "ui.оповещение", …). Первый параметр Кому — вызов серверный (в 1С клиентский). Помимо оповещения есть команды ui.обновитьСписок ({сущность}) и ui.открытьФорму ({вид, сущность, id}) через обычный ОтправитьУведомление.

**Сервисы:**

- `РазмерКэшаСервисов()` — Текущий объём кэша ответов HTTP-сервисов в байтах — для диагностики.
- `СброситьКэшСервисов([ИмяСервиса])` — Сбрасывает кэш ответов HTTP-сервисов и возвращает число выброшенных записей. Вызывайте из ПриЗаписи справочников, чьи данные попадают в кэшируемые страницы, иначе правка будет видна только по истечении ttl (план 126).

**Сообщения и ошибки:**

- `Error(Сообщение)` — Англоязычный синоним ВызватьИсключение: прерывает выполнение и поднимает ошибку (отменяет запись/проведение).
- `ВызватьИсключение(Сообщение) | ВызватьИсключение Сообщение` — Прерывает выполнение и поднимает пользовательскую ошибку с указанным сообщением (перехватывается Попытка…Исключение). Скобки не обязательны: допустима форма-оператор «ВызватьИсключение Сообщение;», в том числе без аргумента внутри блока Исключение.
- `ИнформацияОбОшибке()` — Возвращает Структуру с полями Описание, НомерСтроки, Источник. Доступна только внутри блока Исключение.
- `ОписаниеОшибки()` — Возвращает текст перехваченной ошибки. Доступна только внутри блока Исключение.
- `Сообщить(Текст)` — Выводит сообщение пользователю (в панель сообщений формы или лог фонового задания).

**Строки:**

- `Base64Значение(СтрокаBase64)` — Декодирует строку Base64 в текст. При ошибке возвращает пустую строку.
- `Base64Строка(Текст)` — Кодирует UTF-8 текст в строку Base64.
- `БезопасныйHTML(Строка)` — Очищает HTML санитайзером richtext (вырезает теги script, обработчики событий, схему javascript:) и помечает результат готовой разметкой для ШаблонHTML. Без этой функции значение вставляется как текст — теги экранируются (план 125).
- `ВРег(Строка)` — Переводит все символы строки в верхний регистр.
- `КодироватьСтроку(Строка[, Способ[, Кодировка]])` — Процентное кодирование для URL: значение параметра можно безопасно подставить в адрес. Ошибки аргументов перехватываются через Попытка/Исключение.
- `КодСимвола(Строка[, Позиция])` — Возвращает код Unicode символа в указанной позиции (нумерация с 1) или 0 вне диапазона.
- `Лев(Строка, Длина)` — Возвращает указанное число первых (левых) символов строки.
- `НРег(Строка)` — Переводит все символы строки в нижний регистр.
- `НСтр(ИсходнаяСтрока[, КодЯзыка])` — Выбирает локализованную строку по коду языка. Без явного кода — язык текущего пользователя (в обработчиках страниц/форм; вне запроса "ru"). Если язык не найден среди сегментов, возвращает первый. Удобно для статической части склеенных подписей: НСтр("ru = 'Отчёт за '; en = 'Report for '") + Период.
- `Прав(Строка, Длина)` — Возвращает указанное число последних (правых) символов строки.
- `ПустаяСтрока(Строка)` — Истина, если строка пуста или состоит только из пробельных символов.
- `РаскодироватьСтроку(Строка[, Способ[, Кодировка]])` — Обратное к КодироватьСтроку. Битую percent-последовательность и декодированный невалидный UTF-8 возвращает как исходную строку; неизвестный способ и неподдерживаемая кодировка отклоняются ловимым исключением.
- `РегексЭкранировать(Строка)` — Экранирует спецсимволы регулярного выражения, чтобы строку можно было вставить в шаблон как литерал (план 124).
- `Символ(КодСимвола)` — Возвращает символ по его коду Unicode.
- `СокрЛ(Строка)` — Убирает пробельные символы в начале строки (слева).
- `СокрЛП(Строка)` — Убирает пробельные символы с обоих концов строки.
- `СокрП(Строка)` — Убирает пробельные символы в конце строки (справа).
- `Сред(Строка, Позиция[, Длина])` — Возвращает подстроку, начиная с позиции (нумерация с 1). Без длины — до конца строки.
- `СтрДлина(Строка)` — Возвращает число символов в строке.
- `СтрЗаканчиваетсяНа(Строка, Суффикс)` — Истина, если строка заканчивается указанным суффиксом.
- `СтрЗаменить(Строка, Что, Чем)` — Заменяет все вхождения подстроки «Что» на «Чем».
- `СтрНайти(Строка, Подстрока)` — Возвращает позицию первого вхождения подстроки (нумерация с 1) или 0, если не найдено.
- `СтрНачинаетсяС(Строка, Префикс)` — Истина, если строка начинается с указанного префикса.
- `Строка(Значение)` — Преобразует значение в строку. Ссылка даёт представление (наименование справочника, номер документа), а не идентификатор: сравнивать объекты через Строка() нельзя — представления повторяются. Идентификатор даёт Ссылка.УникальныйИдентификатор().
- `СтрПолучитьСтроку(Строка, НомерСтроки)` — Возвращает строку с указанным номером (нумерация с 1) из многострочного текста или пустую строку.
- `СтрРазделить(Строка, Разделитель)` — Разбивает строку по разделителю и возвращает Массив подстрок.
- `СтрСодержит(Строка, Подстрока)` — Истина, если строка содержит подстроку.
- `СтрСоединить(Массив, Разделитель)` — Соединяет элементы Массива в строку через разделитель.
- `СтрСравнить(Строка1, Строка2)` — Сравнивает две строки без учёта регистра. Возвращает -1, 0 или 1.
- `СтрЧислоВхождений(Строка, Подстрока)` — Возвращает число непересекающихся вхождений подстроки в строку.
- `СтрЧислоСтрок(Строка)` — Возвращает число строк в многострочном тексте (минимум 1).
- `СтрШаблон(Шаблон, Значение1[, Значение2, …])` — Подставляет аргументы в шаблон по позициям %1, %2 и т.д.
- `ТРег(Строка)` — Делает каждое слово с заглавной буквы, остальные буквы строчными.
- `ЧислоПрописью(Сумма[, Валюта])` — Возвращает текстовое представление денежной суммы прописью со склонением единиц и копейками.

**Транзакции:**

- `ЗафиксироватьТранзакцию()` — Фиксирует текущую транзакцию (или освобождает точку сохранения для вложенной).
- `НачатьТранзакцию()` — Открывает транзакцию БД. Вложенный вызов создаёт точку сохранения (SAVEPOINT).
- `ОтменитьТранзакцию()` — Откатывает текущую транзакцию (или к точке сохранения для вложенной).
- `ТранзакцияАктивна()` — Возвращает Истина, если код уже выполняется внутри транзакции БД, включая транзакцию внешнего сервиса или тестовой изоляции.

**Файлы:**

- `ДекодироватьФайл(Текст)` — Возвращает текст в UTF-8, при необходимости перекодируя из Windows-1251.
- `КаталогВременныхФайлов()` — Каталог для временных файлов с завершающим разделителем текущей ОС. В path-песочнице возвращает приватный подкаталог внутри неё; профиль DenyFile запрещает функцию вместе с остальными файловыми возможностями.
- `КопироватьФайл(Откуда, Куда)` — Копирует содержимое файла из одного пути в другой.
- `НайтиФайлы(Путь, Маска)` — Возвращает Массив путей файлов в каталоге, подходящих под маску.
- `ОпубликоватьКартинку(ИдКартинки [, Опции])` — Делает картинку из поля карточки доступной по ссылке без авторизации. Отдельная функция от ОпубликоватьФайл: картинки хранятся не там же, где вложения, и идентификаторы путать нельзя (план 127).
- `ОпубликоватьФайл(ИдВложения [, Опции])` — Делает вложение доступным по ссылке без авторизации и возвращает путь вида /pub/<токен>. Ссылка и есть право на файл: кто её знает, тот скачает. Повторный вызов возвращает ту же ссылку (план 127).
- `ПереместитьФайл(Откуда, Куда)` — Перемещает (переименовывает) файл из одного пути в другой.
- `ПолучитьИмяВременногоФайла([Расширение])` — Имя с 128 случайными битами во временном каталоге. Файл автоматически не создаётся и не удаляется: созданный обработкой файл удалите через УдалитьФайлы; разделители пути, `..`, двоеточие и управляющие символы в расширении отклоняются. DenyFile запрещает функцию.
- `ПолучитьРазделительПути()` — Разделитель каталогов текущей операционной системы.
- `ПрисоединитьФайл(Ссылка, ПутьКФайлу, ИмяФайла = "")` — Прикрепляет файл с диска вложением к записи и возвращает идентификатор вложения. Лимиты размера и типов — как у загрузки через веб-форму.
- `ПутьКВложению(ИдВложения)` — Возвращает путь к файлу вложения в хранилище — для отправки письмом или передачи внешней команде.
- `СнятьПубликациюКартинки(ИдКартинки)` — Отзывает публичную ссылку на картинку: адрес немедленно перестаёт работать.
- `СнятьПубликациюФайла(ИдВложения)` — Отзывает публичную ссылку: адрес немедленно перестаёт работать (404).
- `СоздатьКаталог(Путь)` — Создаёт каталог вместе со всеми отсутствующими родительскими каталогами.
- `СписокВложений(Ссылка)` — Возвращает Массив Структур {ИД, ИмяФайла, Размер, ТипMIME, Загружен} — вложения записи.
- `СсылкаНаКартинку(ИдКартинки)` — Публичная ссылка на картинку или Неопределено, если она не опубликована либо срок истёк.
- `СсылкаНаФайл(ИдВложения)` — Возвращает публичную ссылку на вложение или Неопределено, если оно не опубликовано либо срок публикации истёк.
- `УдалитьВложение(ИдВложения)` — Удаляет вложение записи (файл и метаданные).
- `УдалитьФайлы(Путь)` — Удаляет файл или пустой каталог. Нерекурсивно — дерево каталогов не сносит.

**Формы:**

- `ДобавитьПравилоОформления(Цель, Условие, Поле, Фон[, Цвет[, Жирный[, Курсив]]])` — Добавляет runtime-правило условного оформления managed-формы в текущем обработчике события. Сокращённая форма ДобавитьПравилоОформления(Цель, Условие, Фон) оформляет всю строку. Правило применяется к JSON-ответу события и не записывается в YAML.
- `ОчиститьОформление([Цель])` — Очищает правила условного оформления managed-формы для текущего обработчика события. Без параметра сбрасывает и декларативные правила на время ответа.

**Число:**

- `Acos(Число)` — Возвращает арккосинус (в радианах).
- `Asin(Число)` — Возвращает арксинус (в радианах).
- `Atan(Число)` — Возвращает арктангенс (в радианах).
- `Cos(Угол)` — Возвращает косинус угла (в радианах).
- `Exp(Число)` — Возвращает экспоненту (e в степени аргумента).
- `Log(Число)` — Возвращает натуральный логарифм числа.
- `Log10(Число)` — Возвращает десятичный логарифм числа.
- `Pow(Основание, Степень)` — Возводит основание в указанную степень.
- `Sin(Угол)` — Возвращает синус угла (в радианах).
- `Sqrt(Число)` — Возвращает квадратный корень числа.
- `Tan(Угол)` — Возвращает тангенс угла (в радианах).
- `Абс(Число)` — Возвращает абсолютное значение (модуль) числа.
- `Макс(Число1, Число2)` — Возвращает большее из двух чисел.
- `Мин(Число1, Число2)` — Возвращает меньшее из двух чисел.
- `Окр(Число[, Точность, Режим])` — Округляет число до указанной точности. Режим 0 — округление от нуля (как Окр в 1С), 1 — банковское.
- `Распределить(Сумма, Веса[, Точность])` — Пропорционально распределяет сумму по весам с гарантией сохранения итога. Возвращает Массив той же длины, что и Веса.
- `Цел(Число)` — Возвращает целую часть числа (отбрасывает дробную, усечение к нулю).
- `Число(Значение)` — Преобразует значение в число. Запятая трактуется как десятичный разделитель; при ошибке возвращает 0. Дата даёт ГГГГММДДЧЧММСС; Дата(Число(Д)) восстанавливает календарные компоненты до секунд. Часовой пояс и доли секунды не кодируются; пустая дата и годы вне 0001–9999 дают 0. Это НЕ секунды от эпохи: для интервала бери Д2 - Д1 или РазностьДат, а для сдвига — ДобавитьСекунд/ДобавитьМинут/ДобавитьЧас.

### Методы объектов

**HTTPЗапрос:**

- `HTTPЗапрос.УстановитьЗаголовок(Имя, Значение)` — Устанавливает HTTP‑заголовок запроса.
- `HTTPЗапрос.УстановитьТелоИзСтроки(Текст)` — Задаёт тело запроса из строки.

**HTTPОтвет:**

- `HTTPОтвет.ПолучитьЗаголовок(Имя)` — Возвращает значение заголовка ответа по имени (пустая строка, если нет).
- `HTTPОтвет.ПолучитьТелоКакСтроку()` — Возвращает тело ответа как строку.

**HTTPСервисЗапрос:**

- `Запрос.ФормаДанные()` — Разбирает тело HTML-формы в Соответствие имя → значение. Понимает application/x-www-form-urlencoded и multipart/form-data; тело другого типа (например JSON) — ошибка с указанием фактического типа, для JSON есть Запрос.ТелоJSON(). Поле с файлом в multipart-форме тоже даёт ошибку с именем поля: приём файлов через ФормаДанные не поддерживается. Значения раскодированы: пробелы и кириллица приходят как есть. Для повторяющихся имён берётся первое.

**HTTPСервисОтвет:**

- `HTTPСервисОтвет.ПолучитьТелоКакСтроку()` — Возвращает тело ответа как строку. Нужен обработке kind: test, чтобы проверить разметку, которую отдаёт HTTP-сервис: код состояния у страницы с ошибкой в разметке такой же, как у исправной.

**HTTPСоединение:**

- `HTTPСоединение.ОтправитьДля(Запрос, Метод)` — Отправляет запрос указанным методом (по умолчанию POST) и возвращает ответ.
- `HTTPСоединение.Получить(Запрос)` — Выполняет GET‑запрос по заданному ресурсу и возвращает ответ.

**БлокировкаДанных:**

- `БлокировкаДанных.Добавить(ИмяРегистра)` — Добавляет элемент блокировки по регистру и возвращает его для установки значений измерений.
- `БлокировкаДанных.Заблокировать()` — Устанавливает блокировки по всем добавленным элементам.
- `БлокировкаДанных.Разблокировать()` — Снимает удерживаемые блокировки.

**Движения:**

- `Движения.ИмяРегистра.Добавить()` — Добавляет новую запись движения по регистру и возвращает её для заполнения измерений и ресурсов.
- `Движения.ИмяРегистра.Очистить()` — Удаляет накопленные движения по регистру в текущем проведении.

**Диаграмма.Серии:**

- `Диаграмма.Серии.Добавить()` — Добавляет серию диаграммы и возвращает её.
- `Диаграмма.Серии.Количество()` — Возвращает число серий диаграммы.

**Диаграмма.Серия:**

- `Диаграмма.Серия.УстановитьЗначение(Точка, Значение)` — Задаёт числовое значение серии в указанной точке диаграммы.

**Диаграмма.Точки:**

- `Диаграмма.Точки.Добавить()` — Добавляет точку (категорию) диаграммы и возвращает её.
- `Диаграмма.Точки.Количество()` — Возвращает число точек диаграммы.

**Документы:**

- `Документы.Имя.НайтиПоИдентификатору(UUID)` — Создаёт ссылку документа по UUID, например из результата запроса Ссылка. Существование записи проверяется при ПолучитьОбъект().
- `Документы.Имя.НайтиПоНомеру(Номер)` — Ищет документ по номеру; возвращает ссылку или Неопределено.
- `Документы.Имя.НайтиПоРеквизиту(Реквизит, Значение)` — Ищет документ по значению произвольного реквизита; возвращает ссылку или Неопределено.
- `Документы.Имя.ОтменитьПроведение(Ссылка)` — Отменяет проведение документа: чистит его движения по всем регистрам и снимает признак проведения.
- `Документы.Имя.ПометитьНаУдаление(Ссылка)` — Помечает документ на удаление (для проведённого — с авто‑отменой проведения).
- `Документы.Имя.ПроверитьСовпадениеПоРеквизиту(Реквизит, Значение)` — Safe-match для импорта/синхронизации: возвращает Структуру с полями Статус ("НеНайдено"/"НайденаОдна"/"НайденоНесколько"), Ссылка (только при ровно одном совпадении) и Количество (точное число совпадений).
- `Документы.Имя.СнятьПометку(Ссылка)` — Снимает с документа пометку на удаление.
- `Документы.Имя.Создать()` — Создаёт новый записываемый объект документа; заполните шапку и табличные части, затем Записать()/Провести().
- `Документы.Имя.Удалить(Ссылка)` — Удаляет документ по ссылке; для проводимых документов предварительно очищает движения.

**ЗаписьТекста:**

- `ЗаписьТекста.Закрыть()` — Записывает накопленный буфер в файл на диске и закрывает запись.
- `ЗаписьТекста.Записать(Текст)` — Дописывает текст в буфер без перевода строки.
- `ЗаписьТекста.ЗаписатьСтроку(Текст)` — Дописывает текст в буфер и добавляет перевод строки.
- `ЗаписьТекста.Открыть()` — Открывает запись в файл (сбрасывает буфер); путь задан при создании объекта.

**Запрос:**

- `Запрос.Выполнить()` — Компилирует текст запроса в SQL, выполняет и возвращает Массив строк (каждая строка — Структура полей).
- `Запрос.УстановитьПараметр(Имя, Значение)` — Задаёт значение параметра запроса (подставляется вместо &Имя в тексте).

**КолонкиТаблицыЗначений:**

- `КолонкиТаблицыЗначений.Добавить(Имя)` — Добавляет колонку с указанным именем (тип игнорируется).
- `КолонкиТаблицыЗначений.Количество()` — Возвращает число колонок.
- `КолонкиТаблицыЗначений.Получить(Индекс)` — Возвращает имя колонки по индексу (с нуля) или Неопределено.
- `КолонкиТаблицыЗначений.Удалить(Имя)` — Удаляет колонку по имени.

**Макет:**

- `Макет.Имя()` — Возвращает имя макета.
- `Макет.Область(Имя)` — Возвращает свежую копию именованной области макета для вывода в табличный документ. Синоним — ПолучитьОбласть().

**Массив:**

- `Массив.ВГраница()` — Возвращает индекс последнего элемента (Количество−1); для пустого массива −1.
- `Массив.Вставить(Индекс, Значение)` — Вставляет значение в указанную позицию, сдвигая последующие элементы вправо.
- `Массив.Добавить(Значение)` — Добавляет элемент в конец массива.
- `Массив.Количество()` — Возвращает число элементов массива.
- `Массив.Найти(Значение)` — Возвращает индекс первого совпадения значения или Неопределено, если не найдено.
- `Массив.Очистить()` — Удаляет все элементы массива.
- `Массив.Получить(Индекс)` — Возвращает элемент по индексу (нумерация с нуля); при выходе за границы — Неопределено.
- `Массив.Удалить(Индекс)` — Удаляет элемент по индексу.
- `Массив.Установить(Индекс, Значение)` — Заменяет элемент по индексу новым значением.

**ОбластьТабличногоДокумента:**

- `ОбластьТабличногоДокумента.Высота()` — Возвращает число строк области.
- `ОбластьТабличногоДокумента.Объединить()` — Объединяет все ячейки области в одну.
- `ОбластьТабличногоДокумента.Очистить()` — Очищает содержимое всех ячеек области.
- `ОбластьТабличногоДокумента.Параметр(Имя)` — Возвращает значение параметра (ячейки) области по имени.
- `ОбластьТабличногоДокумента.Параметры()` — Возвращает объект параметров области для доступа к ячейкам через точку (Обл.Параметры.Имя = …).
- `ОбластьТабличногоДокумента.УстановитьПараметр(Имя, Значение)` — Устанавливает значение параметра (ячейки) области по имени.
- `ОбластьТабличногоДокумента.Ширина()` — Возвращает число колонок области.

**ОбъектДокумента:**

- `ОбъектДокумента.Записать()` — Записывает документ (шапку и табличные части) в базу без проведения; возвращает ссылку.
- `ОбъектДокумента.Заполнить(Основание)` — Заполняет документ на основании другого объекта (запускает ОбработкаЗаполнения).
- `ОбъектДокумента.Провести()` — Записывает и проводит документ (запускает ОбработкаПроведения, формирует движения); возвращает ссылку.
- `ОбъектДокумента.Прочитать()` — Перечитывает шапку и табличные части документа из базы.
- `ОбъектДокумента.УстановитьЗначение(Реквизит, Значение)` — Устанавливает значение реквизита шапки по имени.
- `ОбъектДокумента.ЭтоНовый()` — Возвращает Истина, если документ ещё не записан и не прочитан из базы.

**ОбъектСправочника:**

- `ОбъектСправочника.Записать()` — Записывает объект в базу и возвращает ссылку на сохранённую запись.
- `ОбъектСправочника.Прочитать()` — Перечитывает поля объекта из базы (объект должен быть уже записан).
- `ОбъектСправочника.УстановитьЗначение(Реквизит, Значение)` — Устанавливает значение реквизита по имени (альтернатива Объект.Реквизит = Значение).
- `ОбъектСправочника.ЭтоНовый()` — Возвращает Истина, если объект ещё не записан в базу.

**ПисьмоEmail:**

- `ПисьмоEmail.Отправить()` — Отправляет письмо (обязательны заполненные поля Кому и Тема).

**Регекс:**

- `Регекс.Заменить(Текст, Замена)` — Заменяет все совпадения. В строке замены доступны $1…$9 и именованные группы в обеих формах — ${имя} и $имя (без скобок имя читается до первого символа, который не буква, не цифра и не «_»). Литеральный доллар — $$.
- `Регекс.Найти(Текст)` — Первое совпадение: Структура с полями Значение, Позиция (с 1, в символах), Длина, Группы (Массив; [0] — всё совпадение), ГруппыПоИмени (Соответствие). Неопределено, если совпадений нет.
- `Регекс.НайтиВсе(Текст [, Предел])` — Все совпадения — Массив структур того же вида, что у Найти. Без явного предела превышение 10000 совпадений вызывает ошибку, а не молчаливое усечение.
- `Регекс.Разделить(Текст [, Предел])` — Делит текст по совпадениям шаблона и возвращает Массив строк.
- `Регекс.Совпадает(Текст)` — Истина, если в тексте есть хотя бы одно совпадение с шаблоном.

**РегистрыНакопления:**

- `РегистрыНакопления.Имя.ВыбратьПоРегистратору(Регистратор)` — Возвращает движения регистра, сформированные указанным документом‑регистратором.
- `РегистрыНакопления.Имя.Движения()` — Возвращает все движения регистра как Массив строк. Синоним — Выбрать().
- `РегистрыНакопления.Имя.Остатки()` — Возвращает текущие остатки регистра как Массив строк (Структур измерений и ресурсов).

**Соответствие:**

- `Соответствие.Вставить(Ключ, Значение)` — Добавляет пару ключ‑значение; если ключ уже есть, заменяет значение.
- `Соответствие.Количество()` — Возвращает число пар в соответствии.
- `Соответствие.Очистить()` — Удаляет все пары соответствия.
- `Соответствие.Получить(Ключ)` — Возвращает значение по ключу или Неопределено, если ключа нет.
- `Соответствие.Удалить(Ключ)` — Удаляет пару по ключу.

**Справочники:**

- `Справочники.Имя.НайтиПоИдентификатору(UUID)` — Создаёт ссылку справочника по UUID, например из результата запроса Поле.Ссылка. Существование записи проверяется при ПолучитьОбъект().
- `Справочники.Имя.НайтиПоКоду(Код)` — Ищет элемент справочника по коду; возвращает ссылку или Неопределено.
- `Справочники.Имя.НайтиПоНаименованию(Наименование)` — Ищет элемент справочника по наименованию; возвращает ссылку или Неопределено.
- `Справочники.Имя.НайтиПоРеквизиту(Реквизит, Значение)` — Ищет элемент справочника по значению произвольного реквизита; возвращает ссылку или Неопределено.
- `Справочники.Имя.ПроверитьСовпадениеПоРеквизиту(Реквизит, Значение)` — Safe-match для импорта/синхронизации: возвращает Структуру с полями Статус ("НеНайдено"/"НайденаОдна"/"НайденоНесколько"), Ссылка (только при ровно одном совпадении) и Количество (точное число совпадений). Снимает с прикладного кода ручную обработку 0/1/несколько.
- `Справочники.Имя.Создать()` — Создаёт новый записываемый объект справочника; заполните поля и вызовите Записать().
- `Справочники.Имя.Удалить(Ссылка)` — Удаляет элемент справочника по переданной ссылке.

**Ссылка:**

- `Ссылка.ПолучитьОбъект()` — Загружает изменяемый объект по ссылке; после правки полей Записать()/Провести() обновят запись.
- `Ссылка.Пустая()` — Возвращает Истина, если ссылка пустая (без идентификатора).
- `Ссылка.Удалить()` — Удаляет объект, на который указывает ссылка. Требует ссылку с менеджером (полученную через НайтиПо…).
- `Ссылка.УникальныйИдентификатор()` — Возвращает строковый UUID объекта, на который указывает ссылка. Метод есть у ссылки из Записать(), НайтиПо…() и у поля-ссылки объекта, включая строки табличной части. У полей результата запроса методов нет — там поле-ссылка уже строка представления, а идентификатор выбирают разыменованием: ВЫБРАТЬ Контрагент.Ссылка КАК ИдКонтрагента.

**Страница:**

- `Страница.Абзац(Текст)` — Добавляет абзац текста.
- `Страница.График(Заголовок, Вид)` — Добавляет график (ECharts) и возвращает его построитель (Категории/Серия).
- `Страница.ДобавитьСыройHTML(HTML)` — Вставляет произвольный HTML (escape-hatch). Санитизируется консервативно; на ответственности автора.
- `Страница.Заголовок(Текст)` — Добавляет заголовок секции страницы.
- `Страница.Кнопка(Подпись, URL)` — Добавляет кнопку-ссылку (переход по URL). См. также КнопкаДействие.
- `Страница.КнопкаДействие(Подпись, ИмяПроцедуры)` — Добавляет кнопку, вызывающую серверную процедуру-действие (POST /ui/page/<Имя>/action/<Действие>). После действия страница перерисовывается через ПриФормировании; Сообщить() показывает общий бар. Подходит для побочных эффектов: провести, пересчитать, выгрузить.
- `Страница.Показатель(Подпись, Значение, Формат)` — Добавляет плитку-показатель (KPI) с форматированием значения.
- `Страница.Разделитель()` — Добавляет горизонтальный разделитель.
- `Страница.Список(Заголовок)` — Добавляет список и возвращает его построитель (Пункт).
- `Страница.Таблица(Заголовок)` — Добавляет таблицу и возвращает её построитель (Колонки/ДобавитьСтроку).

**Страница.График:**

- `График.Категории(Знач1, Знач2, … | Массив)` — Задаёт подписи оси категорий графика (перечислением или Массивом).
- `График.Серия(Имя, Массив)` — Добавляет серию данных графика.

**Страница.Список:**

- `Список.Пункт(Текст, URL)` — Добавляет пункт списка с необязательной ссылкой.

**Страница.Строка:**

- `Строка.Ссылка(Колонка, URL)` — Делает ячейку кликабельной ссылкой.
- `Строка.Установить(Колонка, Значение)` — Устанавливает значение ячейки строки таблицы по имени колонки.

**Страница.Таблица:**

- `Таблица.ДобавитьСтроку()` — Добавляет строку и возвращает её построитель (Установить/Ссылка).
- `Таблица.Колонки(Имя1, Имя2, …)` — Задаёт заголовки колонок таблицы.

**Структура:**

- `Структура.Вставить(Ключ, Значение)` — Добавляет или заменяет свойство по строковому ключу.
- `Структура.Количество()` — Возвращает число свойств структуры.
- `Структура.Свойство(Ключ)` — Возвращает значение свойства по ключу или Неопределено, если свойства нет.
- `Структура.Удалить(Ключ)` — Удаляет свойство по ключу.

**ТаблицаЗначений:**

- `ТаблицаЗначений.ВыгрузитьКолонку(Колонка)` — Возвращает значения указанной колонки всех строк как Массив.
- `ТаблицаЗначений.Добавить()` — Добавляет новую строку и возвращает её для заполнения колонок.
- `ТаблицаЗначений.ЗагрузитьКолонку(Массив, Колонка)` — Заполняет (создавая при необходимости) колонку значениями из массива по порядку строк.
- `ТаблицаЗначений.Итог(Колонка)` — Возвращает сумму числовых значений указанной колонки по всем строкам.
- `ТаблицаЗначений.Количество()` — Возвращает число строк таблицы значений.
- `ТаблицаЗначений.Колонки()` — Возвращает коллекцию колонок таблицы значений.
- `ТаблицаЗначений.Найти(Значение, Колонка)` — Возвращает первую строку с совпадающим значением или Неопределено.
- `ТаблицаЗначений.НайтиСтроки(Отбор)` — Возвращает Массив строк, у которых все поля совпали со структурой отбора.
- `ТаблицаЗначений.Очистить()` — Удаляет все строки таблицы значений.
- `ТаблицаЗначений.Получить(Индекс)` — Возвращает строку по индексу (с нуля) или Неопределено при выходе за границы.
- `ТаблицаЗначений.Свернуть(КолонкиГруппировки, КолонкиСуммирования)` — Группирует строки по группировочным колонкам, суммируя числовые колонки суммирования.
- `ТаблицаЗначений.Сортировать(Колонки)` — Сортирует строки по списку колонок; направление Убыв/Возр после имени, по умолчанию по возрастанию.
- `ТаблицаЗначений.Удалить(Индекс)` — Удаляет строку по индексу.

**ТабличнаяЧасть:**

- `ТабличнаяЧасть.Добавить()` — Добавляет новую строку табличной части и возвращает её для заполнения колонок.
- `ТабличнаяЧасть.Количество()` — Возвращает число строк табличной части.
- `ТабличнаяЧасть.Очистить()` — Удаляет все строки табличной части.

**ТабличныйДокумент:**

- `ТабличныйДокумент.Вывести(Область)` — Выводит область в текущую позицию и переходит на следующую строку.
- `ТабличныйДокумент.Выровнять(Область, Гориз, Верт)` — Устанавливает горизонтальное и вертикальное выравнивание ячеек области.
- `ТабличныйДокумент.Высота()` — Возвращает число строк документа.
- `ТабличныйДокумент.ВысотаСтроки(Строка, Высота)` — Устанавливает высоту указанной строки.
- `ТабличныйДокумент.ЗакончитьСтраницу()` — Завершает текущую страницу при печати.
- `ТабличныйДокумент.Записать(ИмяФайла, ТипФайла)` — Сохраняет документ в файл указанного типа и возвращает его текстовое/HTML‑представление.
- `ТабличныйДокумент.Нарисовать(Объект)` — Размещает в документе рисунок (например диаграмму).
- `ТабличныйДокумент.Область(Верх, Лево, Низ, Право)` — Возвращает область документа по координатам (нумерация строк и колонок с 1).
- `ТабличныйДокумент.Объединить(Верх, Лево, Низ, Право)` — Объединяет ячейки в прямоугольнике по координатам.
- `ТабличныйДокумент.Очистить()` — Очищает всё содержимое документа.
- `ТабличныйДокумент.ПовторитьПриПечати(Область, Признак)` — Задаёт область, повторяемую на каждой странице при печати (шапка/подвал).
- `ТабличныйДокумент.Показать()` — Помечает документ к показу пользователю (открытие печатной формы).
- `ТабличныйДокумент.ПолучитьОбласть(Имя)` — Возвращает ранее именованную область документа по имени.
- `ТабличныйДокумент.ПолучитьРисунок(Имя)` — Возвращает рисунок документа по имени.
- `ТабличныйДокумент.Присоединить(Область)` — Выводит область справа от ранее выведенной (без перехода на новую строку).
- `ТабличныйДокумент.ПроверитьВывод(Область)` — Проверяет, помещается ли область на текущей странице при печати.
- `ТабличныйДокумент.РазделительСтраниц()` — Вставляет принудительный разрыв страницы в текущей позиции.
- `ТабличныйДокумент.ТекущаяКолонка()` — Возвращает номер текущей колонки вывода (с 1).
- `ТабличныйДокумент.ТекущаяСтрока()` — Возвращает номер текущей строки вывода (с 1).
- `ТабличныйДокумент.УдалитьОбласть(Область)` — Удаляет ранее выведенную область из документа.
- `ТабличныйДокумент.УстановитьИмяОбласти(Область, Имя)` — Присваивает области имя, по которому её можно получить через ПолучитьОбласть().
- `ТабличныйДокумент.Ширина()` — Возвращает число колонок документа.
- `ТабличныйДокумент.ШиринаКолонки(Колонка, Ширина)` — Устанавливает ширину указанной колонки.
- `ТабличныйДокумент.Ячейка(Строка, Колонка)` — Возвращает объект ячейки по координатам для чтения/настройки.

**Файл:**

- `Файл.Существует()` — Проверяет наличие файла на диске и возвращает Истина/Ложь.

**ЧтениеТекста:**

- `ЧтениеТекста.Закрыть()` — Закрывает чтение файла.
- `ЧтениеТекста.Открыть()` — Открывает (читает в память) файл, путь к которому задан при создании объекта.
- `ЧтениеТекста.Прочитать()` — Возвращает всё содержимое открытого файла как строку.
- `ЧтениеТекста.ПрочитатьСтроку()` — Возвращает очередную строку файла или Неопределено по достижении конца.

**ШаблонHTML:**

- `ШаблонHTML.Заполнить(Данные)` — Подставляет данные в шаблон и возвращает готовый HTML. Значения экранируются автоматически; чтобы вставить готовую разметку, оберните её в БезопасныйHTML(). Отсутствующее поле даёт пустую строку. В шаблоне доступны функции «дата» и «число»: {{дата .Дата "02.01.2006"}}, {{число .Сумма 2}}. Имена функций пишутся в любом из регистров («дата», «Дата», «ДАТА»), английские синонимы — date и number.

**ЭлементБлокировки:**

- `ЭлементБлокировки.УстановитьЗначение(Измерение, Значение)` — Задаёт значение измерения для элемента блокировки (сужает блокируемый набор).

### Язык запросов

- `<поле> В (&Параметр)` — Проверяет вхождение значения в список или массив-параметр. Массив &Param разворачивается в перечень плейсхолдеров.
- `ВНУТРЕННЕЕ СОЕДИНЕНИЕ <источник> ПО <условие>` — Внутреннее соединение: только строки, удовлетворяющие условию с обеих сторон.
- `УПОРЯДОЧИТЬ ПО <поле> ВОЗР` — Направление сортировки по возрастанию (применяется в УПОРЯДОЧИТЬ ПО).
- `ВЫБОР КОГДА <условие> ТОГДА <значение> [ИНАЧЕ <значение>] КОНЕЦ` — Условное выражение в запросе (аналог CASE/WHEN/THEN/ELSE/END в SQL).
- `ВЫБРАТЬ [РАЗЛИЧНЫЕ] <поля> ИЗ <источник>` — Открывает запрос; перечисляет поля для выборки. РАЗЛИЧНЫЕ исключает дубли строк. Ссылочное поле (Контрагент, Склад) приходит в результат представлением — наименованием справочника или номером документа, — поэтому сравнивать по нему объекты нельзя: представления повторяются. Идентификатор выбирают явно: свой — ВЫБРАТЬ Ссылка, чужой — ВЫБРАТЬ Склад.Ссылка КАК ИдСклада. Полученный UUID — строка без методов; объект по нему даёт Справочники.X.НайтиПоИдентификатору(...).
- `ГДЕ <условие>` — Фильтрует строки источника до группировки; условие может включать &параметры.
- `<поле> ЕСТЬ ПУСТО` — Проверяет, является ли значение NULL (ЕСТЬ ПУСТО → IS NULL).
- `ЕстьNULL(Значение, ЗначениеЕслиNULL)` — Подставляет второе значение, если первое NULL (SQL COALESCE). Нужен после левых соединений и виртуальных таблиц остатков.
- `<условие1> И <условие2>` — Логическое И — оба условия должны быть истинны.
- `ИЗ <ТипИсточника>.<Имя>[.ВиртуальнаяТаблица(...)]` — Задаёт источник данных: справочник, документ, регистр или виртуальная таблица.
- `<условие1> ИЛИ <условие2>` — Логическое ИЛИ — хотя бы одно условие должно быть истинно.
- `ИМЕЯ <условие на агрегат>` — Фильтрует сгруппированные строки по условию над агрегатными функциями.
- `<выражение> КАК <Псевдоним>` — Присваивает псевдоним полю или подзапросу в результате выборки.
- `КОЛИЧЕСТВО(<поле>|*)` — Агрегатная функция — возвращает количество строк (или ненулевых значений поля) в группе.
- `ЛЕВОЕ СОЕДИНЕНИЕ <источник> ПО <условие>` — Левое внешнее соединение: все строки левого источника, плюс совпавшие строки правого.
- `МАКСИМУМ(<поле>)` — Агрегатная функция — возвращает максимальное значение поля в группе.
- `<поле> МЕЖДУ <начало> И <конец>` — Проверяет, входит ли значение в указанный диапазон, включая обе границы.
- `МИНИМУМ(<поле>)` — Агрегатная функция — возвращает минимальное значение поля в группе.
- `НЕ <условие>` — Логическое отрицание условия.
- `РегистрНакопления.<Имя>.Обороты(&Начало, &Конец, <Периодичность>, <Условие>)` — Виртуальная таблица оборотов регистра накопления за период. Третий аргумент — периодичность (День/Неделя/Месяц/Квартал/Год/Запись); четвёртый — условие фильтра. Ресурсы получают суффиксы Приход/Расход/Оборот.
- `РегистрБухгалтерии.<Имя>.Обороты(&Начало, &Конец, <Условие>)` — Виртуальная таблица оборотов по счетам регистра бухгалтерии за период; ресурсы получают суффиксы _дт и _кт. Третий аргумент — условие фильтра.
- `ОБЪЕДИНИТЬ [ВСЕ]` — Объединяет результаты нескольких запросов; ВСЕ сохраняет дубли.
- `РегистрНакопления.<Имя>.Остатки(&НаДату, <Условие>)` — Виртуальная таблица остатков регистра накопления на указанную дату; поля-ресурсы получают суффикс Остаток. Второй аргумент — необязательное условие фильтра.
- `РегистрБухгалтерии.<Имя>.Остатки(&НаДату, <Условие>)` — Виртуальная таблица остатков по счетам регистра бухгалтерии на дату; ресурсы получают суффиксы _дт, _кт и Остаток. Второй аргумент — условие фильтра по счёту или субконто.
- `РегистрНакопления.<Имя>.ОстаткиИОбороты(&Начало, &Конец, <Условие>)` — Виртуальная таблица остатков и оборотов регистра накопления: начальный остаток, приход, расход и конечный остаток за период.
- `ВЫБРАТЬ [РАЗЛИЧНЫЕ] ПЕРВЫЕ <N> <поля>` — Ограничивает простой запрос первыми N строками (SQL LIMIT). Сочетается с РАЗЛИЧНЫЕ и УПОРЯДОЧИТЬ ПО; в ветвях ОБЪЕДИНИТЬ и подзапросах пока отклоняется явно. N = 0 допустимо и даёт пустой результат (как в 1С), отрицательное — ошибка.
- `ПОЛНОЕ СОЕДИНЕНИЕ <источник> ПО <условие>` — Полное внешнее соединение: все строки обоих источников; несовпавшие поля заполняются NULL.
- `ПРАВОЕ СОЕДИНЕНИЕ <источник> ПО <условие>` — Правое внешнее соединение: все строки правого источника, плюс совпавшие строки левого.
- `ЕСТЬ ПУСТО | НЕ ЕСТЬ ПУСТО` — Ключевое слово NULL в языке запросов; используется с ЕСТЬ для проверки на пустое значение.
- `ВЫБРАТЬ РАЗЛИЧНЫЕ <поля>` — Убирает дублирующиеся строки результата запроса.
- `СГРУППИРОВАТЬ ПО <поле1>[, <поле2>...]` — Группирует строки по указанным полям; обязательно при использовании агрегатных функций.
- `СРЕДНЕЕ(<поле>)` — Агрегатная функция — возвращает среднее арифметическое значений поля в группе.
- `РегистрСведений.<Имя>.СрезПервых(&НаДату, <Условие>)` — Виртуальная таблица среза первых записей периодического регистра сведений начиная с указанной даты.
- `РегистрСведений.<Имя>.СрезПоследних(&НаДату, <Условие>)` — Виртуальная таблица среза последних записей периодического регистра сведений на указанную дату. Для непериодических регистров дата игнорируется.
- `СУММА(<поле>)` — Агрегатная функция — возвращает сумму значений поля в группе.
- `УПОРЯДОЧИТЬ ПО <поле> УБЫВ` — Направление сортировки по убыванию (применяется в УПОРЯДОЧИТЬ ПО).
- `УПОРЯДОЧИТЬ ПО <поле> [ВОЗР|УБЫВ][, ...]` — Сортирует результат запроса по одному или нескольким полям.

## ИИ-помощник (LLM)

Функции доступны в модулях DSL (`.os`) при условии, что ИИ-помощник настроен в базе
(Настройки → ИИ-помощник: провайдер, модель, ключ API). Если помощник не настроен,
любая из функций выбрасывает ошибку — перехватывай через `Попытка/Исключение`.

### Функции

**`ЗапросИИ(Промпт [, Параметры])`** (псевдоним: `AIQuery`)

Отправляет текстовый промпт и возвращает ответ строкой. Второй необязательный
аргумент — `Структура` с ключами:

| Ключ | Тип | Описание |
|------|-----|---------|
| `задача` | строка | Профиль маршрутизации (см. ниже). По умолчанию — `"анализ"`. |
| `система` | строка | Системная инструкция (system prompt). |
| `формат` | строка | Передай `"json"` для запроса строгого JSON-ответа. |
| `температура` | число | Температура генерации (0–1). |

**`ЗапросИИДжейсон(Промпт [, Параметры])`** (псевдоним: `AIQueryJSON`)

Аналогично `ЗапросИИ`, но автоматически включает режим строгого JSON (то же, что
передать `формат: "json"` вручную). Возвращает строку — парси через `ЗначениеИзJSON()`.

**`РаспознатьДокумент(Путь, Промпт)`** (псевдоним: `RecognizeDocument`)

Vision-запрос: читает файл **с диска** по пути `Путь`, определяет MIME по расширению
(`.png`, `.jpg`, `.webp`, `.gif`, `.pdf`), кодирует в base64 и передаёт модели вместе
с текстовым `Промпт`ом. Профиль задачи — `"документы"`. Используй для обработки
файлов на сервере; для загруженных пользователем вложений (уже в памяти) предпочитай
`РаспознатьИзображение`.

**`РаспознатьИзображение(ДанныеBase64, ТипMIME, Промпт)`** (псевдоним: `RecognizeImage`)

Vision-запрос по данным **из памяти**: принимает base64-строку и MIME-тип напрямую,
без обращения к диску. Профиль задачи — `"документы"`. Рекомендован для обработки
файлов, загруженных пользователем через UI (вложения, сканы). Если `ТипMIME` пуст,
используется `"image/png"`.

### Профили задач (`задача`)

Параметр `задача` выбирает именованный профиль маршрутизации из конфига ИИ-помощника.
Профиль определяет, какая модель и endpoint используются. Стандартные профили:

| Имя | Назначение |
|-----|-----------|
| `анализ` | Текстовый анализ, суммаризация, классификация (по умолчанию для `ЗапросИИ`). |
| `документы` | Распознавание документов и изображений, требует vision-модель (по умолчанию для `РаспознатьДокумент/Изображение`). |
| `чат` | Диалоговый интерфейс (определяется в конфиге базы). |
| `конфигуратор` | Помощь по разработке конфигурации (определяется в конфиге базы). |

Неизвестный профиль даёт ошибку, если в конфиге не задан `default_profile`.

### Обработка ошибок

Все функции при сбое выбрасывают ошибку (`userError`), перехватываемую через
`Попытка/Исключение`. Типичные причины: помощник не настроен, неизвестный профиль,
сетевая ошибка или ошибка API.

### Примеры

```
// Простой текстовый запрос (профиль "анализ" по умолчанию):
Рез = ЗапросИИ("Сформулируй короткое описание товара: " + Товар.Наименование);
Товар.Описание = Рез;
Товар.Записать();

// JSON-ответ со структурой параметров:
Парам = Новый Структура;
Парам.задача = "анализ";
Парам.система = "Отвечай строго в формате JSON: {\"теги\": [...], \"категория\": \"...\"}";
Парам.температура = 0.2;
ДанныеJSON = ЗапросИИДжейсон("Классифицируй товар: " + Товар.Наименование, Парам);
Данные = ЗначениеИзJSON(ДанныеJSON);
Товар.Категория = Данные["категория"];

// Попытка перехвата ошибки:
Попытка
    Рез = ЗапросИИ("Переведи на английский: " + Текст);
Исключение
    Сообщить("ИИ недоступен: " + ОписаниеОшибки());
    Рез = Текст;
КонецПопытки;

// Распознавание загруженного пользователем файла:
// (ДанныеB64, МимТип — из объекта вложения формы)
Рез = РаспознатьИзображение(Вложение.ДанныеBase64, Вложение.ТипMIME,
    "Извлеки: поставщик, номер счёта, сумма, дата");
```

## Тип объекта конфигурации в модуле

`ТипЗнч()` называет объекты конфигурации квалифицированно, как в 1С, а
`Тип()` понимает те же строки:

| Значение | `ТипЗнч()` |
|---|---|
| объект документа | `Документ.РеализацияТоваров` |
| ссылка на документ | `ДокументСсылка.РеализацияТоваров` |
| объект справочника | `Справочник.Номенклатура` |
| ссылка на справочник | `СправочникСсылка.Номенклатура` |

- **Объект и ссылка — разные типы.** Сравнение объекта с `Тип("ДокументСсылка.X")`
  всегда Ложь, и молча: тихая Ложь — обычная цена ошибки в имени типа, поэтому
  проверяй ветку на реальных данных, а не глазами.
- **`ОбработкаЗаполнения(ДанныеЗаполнения)` получает основание ОБЪЕКТОМ** — у него
  доступны реквизиты и табличные части. Источник распознают по
  `Тип("Документ.X")`; ссылка на источник берётся псевдо-реквизитом
  `ДанныеЗаполнения.Ссылка`. Модуль из 1С, сравнивающий с
  `ДокументСсылка.X`, надо править — либо на `Документ.X`, либо на сравнение
  с `ДанныеЗаполнения.Ссылка`.
- **Имя объекта пишется как в конфигурации.** Регистр префикса не важен
  (`тип("документ.X")` — то же самое), регистр имени — важен: метаданных у
  `Тип()` нет, а сравнение строк регистрозависимо.
- Написание объекта из 1С (`ДокументОбъект.X`, `СправочникОбъект.X`) принимается
  синонимом — перенесённый модуль сравнивается с тем же маркером.

```
Процедура ОбработкаЗаполнения(ДанныеЗаполнения)
  Если ТипЗнч(ДанныеЗаполнения) = Тип("Документ.ЗаказПокупателя") Тогда
    ЭтотОбъект.Покупатель = ДанныеЗаполнения.Покупатель;
    Для Каждого Стр Из ДанныеЗаполнения.Товары Цикл
      Нов = ЭтотОбъект.Товары.Добавить();
      Нов.Номенклатура = Стр.Номенклатура;
    КонецЦикла;
  ИначеЕсли ТипЗнч(ДанныеЗаполнения) = Тип("Документ.СчётНаОплату") Тогда
    ЭтотОбъект.Основание = ДанныеЗаполнения.Ссылка;
  КонецЕсли;
КонецПроцедуры
```

## Запросы — частые ошибки (проверь перед коммитом)

`onebase check` ловит синтаксис и компиляцию запроса, но **не исполнение** и **не
семантику сравнений** — проверяй результат виджета/отчёта на реальных данных. Типовые ловушки:

- **Виртуальные таблицы — только со скобками:** `РегистрНакопления.X.Остатки(...)`, `.Обороты(...)`, `.СрезПоследних(...)`.
  Без `()` суффикс уходит в имя физической таблицы → `no such table`.
- **HAVING — деепричастие `ИМЕЯ`** (не `ИМЕЮ`). Неизвестное слово молча уходит в SQL как
  идентификатор → синтаксическая ошибка в рантайме.
- **Квалифицируй колонку `Объект.Поле`,** если ссылаешься на reference-поле, чей каталог имеет
  одноимённую колонку (`Статус`, `Дата`, `Наименование`) — иначе `ambiguous column` от авто-JOIN.
- **Навигация по ссылке — ровно один уровень.** `Ссылка.Поле` и `Источник.Ссылка.Поле`
  разворачиваются в авто-JOIN; `А.Б.В.Поле` — нет, это ошибка компиляции. Глубже — либо
  выбирай ссылку целиком и дочитывай вторым запросом, либо соединяй таблицу явно
  через `СОЕДИНЕНИЕ`.
- **Служебное поле периода регистра в запросах — `period`** (snake_case), движение — `вид_движения`.
- **Ссылка в результате запроса — ПРЕДСТАВЛЕНИЕ, а не идентификатор.** Поле-ссылка
  (`Контрагент`, `Товар`) приходит наименованием справочника или номером
  документа, поэтому сравнивать по нему объекты нельзя — названия повторяются.
  Идентификатор выбирают явно: свой — `ВЫБРАТЬ Ссылка`, чужой —
  `ВЫБРАТЬ Склад.Ссылка КАК ИдСклада`. Полученный UUID — строка без методов;
  чтобы получить объект, зови `Справочники.X.НайтиПоИдентификатору(...)`.
  То же правило вне запросов: `Строка(Ссылка)` даёт представление, а
  `Ссылка.УникальныйИдентификатор()` — UUID; у поля-ссылки ОБЪЕКТА (в том числе
  в строке табличной части) метод есть, у поля результата запроса — нет.
- **Числовые поля на SQLite сравниваются/сортируются как строки.** `number` хранится как TEXT
  (точный decimal), поэтому `ГДЕ Цена > 100`, `... < Н.МинимальныйОстаток`, `УПОРЯДОЧИТЬ ПО Сумма`
  коэрсятся к строковому сравнению («10» < «9»). Оборачивай сырую колонку:
  `CAST(Н.МинимальныйОстаток AS NUMERIC)` — **не** `AS INTEGER` (усекает дробь). Агрегаты
  `СУММА(...)` уже числовые, их кастить не нужно.
- **Скалярные 1С-функции работают и в запросах** (имена совпадают со скриптовым DSL):
  математика `ОКР(x[, n])`, `АБС(x)`, `ЦЕЛ(x)` (усечение к нулю); даты
  `Год/Месяц/День(x)`, `НачалоДня/НачалоМесяца/НачалоГода(x)`. Транслируются в
  нативный SQL под диалект — сырое русское имя в БД не уходит.

## Схема метаданных (основное)

- **Виды объектов:** справочник, документ, регистр накопления, регистр сведений,
  перечисление, константа, отчёт, обработка, подсистема, журнал, виджет.
- **Типы полей:** `string`, `number`, `date`, `bool`, `text`, `reference:<Объект>`, `enum:<Перечисление>`.
- У документов есть проведение (`posting`) и движения по регистрам; у справочников —
  иерархия (`hierarchical`), предопределённые элементы, ввод на основании (`based_on`).
- **Этапы (`stages`).** Если у сущности объявлен блок `stages` (поле-перечисление,
  `order`, `transitions`), состояние объекта меняется ТОЛЬКО объявленным переходом.
  Проверка стоит в слое хранения, поэтому её не обойти ни формой, ни `Записать()`,
  ни REST: при `enforce: strict` недопустимый переход — ошибка записи, при
  `enforce: warn` (умолчание) — предупреждение в логе. Новый объект создаётся на
  ПЕРВОМ этапе из `order`. Не переставляй этап через промежуточные значения
  «чтобы пройти маршрут»: каждый шаг попадёт в историю переходов, по которой
  считается отчёт «где застряло». Маршрут конкретной сущности — в
  `onebase describe` (блок `stages`).

Точную структуру конкретной базы смотри через `onebase describe`.

## Безопасность

Инструменты разработчика (консоль кода/запросов, отладчик, `procrun`) — для
**разработки**. На опубликованной (задеплоенной) базе debug-API недоступен без
внутреннего токена, а консоли требуют сессию администратора. Не полагайся на их
наличие в проде; данные мутирующие операции (запись/проведение) — только явно.
