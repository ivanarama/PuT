name: ВыручкаМесяца
type: kpi
title: Выручка за месяц
format: money
params:
  Начало: "{{today|start_of_month}}"
query: |
  ВЫБРАТЬ СУММА(Выручка) КАК Значение
  ИЗ РегистрНакопления.ВаловаяПрибыль
  ГДЕ period >= &Начало
