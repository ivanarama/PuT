name: Демо
description: Роль для демо-доступа — чтение и создание без удаления
permissions:
  catalogs:
    Контрагент: [read, write]
    Номенклатура: [read, write]
    Склад: [read, write]
    Касса: [read, write]
    ТипЦен: [read, write]
  documents:
    Счёт: [read, write]
    РеализацияТоваров: [read, write, post, unpost]
    ПоступлениеТоваров: [read, write, post, unpost]
    СписаниеТоваров: [read, write, post, unpost]
    ОприходованиеТоваров: [read, write, post, unpost]
    ПеремещениеТоваров: [read, write, post, unpost]
    ПриходныйКассовыйОрдер: [read, write, post, unpost]
    РасходныйКассовыйОрдер: [read, write, post, unpost]
    ПоступлениеНаРасчётныйСчёт: [read, write, post, unpost]
    СписаниеСРасчётногоСчёта: [read, write, post, unpost]
    УстановкаЦен: [read, write, post, unpost]
  registers:
    Взаиморасчеты: [read]
    ОстаткиТоваров: [read]
    ПартииТоваров: [read]
    ДенежныеСредства: [read]
  reports:
    ОстаткиТоваров: [run]
    ДвиженияТоваров: [run]
    ВаловаяПрибыль: [run]
    Взаиморасчеты: [run]
    ОстаткиДенежныхСредств: [run]
    ДвижениеДенежныхСредств: [run]
    ВедомостьПартий: [run]
    АктСверки: [run]
    ПродажиПоКонтрагентам: [run]
    ОборотПоСкладам: [run]
    ОплатыРеализаций: [run]
    ОплатыПоступлений: [run]
    ЦеныНоменклатуры: [run]
