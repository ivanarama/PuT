# План: АРМы (рабочие места) для конфигурации PuT

Цель — добавить поверх готового торгового ядра PuT набор **АРМов** (автоматизированных
рабочих мест): окон, где сотрудник одной-двумя кнопками собирает данные из регистров и
пакетно создаёт учётные документы. Подход и грабли выверены на конфигурации «Кондитерка»
(5 АРМов), здесь адаптированы под **торговлю с FIFO, резервами и взаиморасчётами**.

> **Принцип слоя.** АРМы — надстройка, добавляется ПОСЛЕ наполнения демо-данными. Каждый
> АРМ — документ с `posting: false` (сам движений не делает) + управляемая форма с
> кнопками. Удаление слоя АРМов не ломает учёт.

---

## 0. Предусловия: текущее состояние базы PuT

`onebase check` сейчас даёт **12 ошибок** — план их учитывает.

**A. Блокер для АРМ Приёмки и взаиморасчётов поставщика.**
`src/поступлениетоваров.posting.os:81` — запрос
`ТоварыВПути.Остатки(&МоментВремени, (Номенклатура, Склад, ЗаказПоставщику))` падает с
`SQL logic error: row value misused`. Причина: **второй аргумент-кортеж измерений** у
`.Остатки()` не поддерживается компилятором — SQLite принимает `(a, b, c)` за row value.
**Фикс** (убрать кортеж, измерения и так доступны в SELECT/ГДЕ):
```
ИЗ РегистрНакопления.ТоварыВПути.Остатки(&МоментВремени)
ГДЕ ЗаказПоставщику = &Заказ И Склад = &Склад
```
Без этого фикса проведение `ПоступлениеТоваров` падает → АРМ Приёмки (3.3) не закроет товары
в пути, не создаст партии FIFO и долг поставщику. **Устранить до реализации АРМ Приёмки/Кассы.**

**B. Косметика (11 шт).** Роли ссылаются на обработки по именам `выгрузка_в_excel`,
`импорт_номенклатуры`, `реализация_из_счёта` и т.п., которых нет под этими именами
(обработки зарегистрированы под другими `name:`). На АРМы не влияет; привести ссылки ролей
в соответствие зарегистрированным именам обработок.

---

## 1. Базовый паттерн (как устроен каждый АРМ)

1. **Документ-АРМ** `documents/<имя>.yaml`, `posting: false`, с numerator, шапкой и одной-двумя
   табличными частями (рабочие ТЧ, не учётные).
2. **Управляемая форма** `forms/<имя-в-нижнем-регистре>/формаобъекта.form.yaml`
   (+ одноимённый `.form.os`). ТЧ рендерится SlickGrid по умолчанию.
3. **Кнопки** (`kind: Кнопка`, событие `Нажатие` → процедура в `.form.os`). Подбор —
   две фазы: `Нажатие` (`ПоказатьПодбор`) + `Выбор` (`ПодборРезультат`).
4. **Логика кнопок** запрашивает регистры/утилиты, заполняет рабочие ТЧ, затем создаёт и
   проводит учётные документы (`Документы.X.Создать()` → `Записать()` → `Провести()`).

### Что переиспользуем из ядра (НЕ дублировать!)
Экспортные функции `src/утилиты.module.os` зовём прямо из `.form.os` АРМа:

| Функция | Назначение |
|---|---|
| `СвободныеОстаткиСклада(Склад, Орг, СвойЗаказ, МВ)` | Массив `{Номенклатура, Свободно}` = остаток − чужие резервы |
| `ПолучитьЦену(Номенклатура, ТипЦен, Дата)` | цена по типу (хранимая/расчётная с наценкой) |
| `НепогашенныйДолгОснования(Осн, Вид, ЭтотДок)` | непогашенный долг по документу-основанию |
| `СуммаНДСстроки(Сумма, СтавкаНДС, Способ)` | НДС строки (Способ: `ВТомЧисле`/`Сверху`) |
| `ОрганизацияПоУмолчанию()`, `СкладПоУмолчанию()` | значения по умолчанию из настроек |

### Ключевое упрощение
**`ПриЗаписи` каждого товарного документа сам досчитывает цену, НДС и суммы**
(`реализациятоваров.os`, `счёт.os`, и т.д.: если `Цена = 0` и задан `ТипЦенПродажи` →
`ПолучитьЦену`; НДС из `СтавкаНДС` номенклатуры; `this.Сумма/СуммаНДС/СуммаБезНДС`).
Поэтому **АРМ при создании документа заполняет только `Номенклатура` и `Количество`**
(+ шапку), а цены/НДС/себестоимость/FIFO/резервы/взаиморасчёты делает запись и проведение.

---

## 2. Карта данных: какой регистр питает какой АРМ

| Регистр (тип) | Измерения → ресурсы | Используют АРМы |
|---|---|---|
| **ОстаткиТоваров** (накопл.) | Номенклатура, Склад, Организация → Количество, Сумма | Продавец, Инвентаризация, Закупки |
| **РезервыТоваров** (накопл.) | Номенклатура, Склад, ЗаказПокупателя → Количество | Закупки, Отгрузка, (свободный остаток) |
| **ТоварыВПути** (накопл.) | Номенклатура, Склад, ЗаказПоставщику → Количество, Сумма | Закупки, Приёмка |
| **ПартииТоваров** (накопл., FIFO) | Номенклатура, Склад, ДокПоступления, ДатаПоставки, Орг → Количество, Сумма | (через `СвободныеОстаткиСклада`) |
| **Взаиморасчеты** (накопл.) | Контрагент, Организация → Сумма (+атрибут ТипКонтрагента) | Касса/Оплаты |
| **ДенежныеСредства** (накопл.) | Касса, Организация → Сумма | Касса (контроль), дашборды |
| **ЦеныНоменклатуры** (свед.) | Номенклатура, ТипЦен → Цена | через `ПолучитьЦену` |

> **Запрос остатков накопительного регистра** (в PuT — фильтр во ВНЕШНЕМ `ГДЕ`, в отличие
> от регистра бухгалтерии «Кондитерки», где фильтр внутри скобок):
> ```
> ВЫБРАТЬ Номенклатура, КоличествоОстаток
> ИЗ РегистрНакопления.ОстаткиТоваров.Остатки(&МоментВремени)
> ГДЕ Склад = &Склад И КоличествоОстаток > 0
> ```

---

## 3. Набор АРМов (6)

### 3.1 ⭐ АРМ Продавца (рабочее место менеджера продаж) — ФЛАГМАН
**Кто:** продавец / менеджер. **Цель:** оформить продажу с подбором по свободному остатку
и ценам, сразу с оплатой.

**Документ-АРМ `РабочееМестоПродаж`** (posting:false): шапка — Организация, Покупатель,
Склад, ТипЦенПродажи, Касса, ОплатитьСразу (bool). ТЧ **Товары**: Номенклатура, Свободно,
Количество, Цена, Сумма.

**Форма — кнопки и алгоритм:**
1. **🔍 Подбор товара** — фаза 1 `ПоказатьПодбор`: данные = `СвободныеОстаткиСклада(Склад,
   Орг, Неопределено, ТекущаяДата())`, по каждой позиции цена = `ПолучитьЦену(Ном,
   ТипЦенПродажи, ТекущаяДата())`; колонки {Наименование, Свободно, Цена, Количество(ред.)}.
   Фаза 2 `Выбор`: выбранные строки → ТЧ Товары (Количество, Цена, Свободно).
2. **💰 Оформить продажу** — создать `РеализацияТоваров` (Покупатель, Склад, ТипЦенПродажи;
   строки Номенклатура+Количество) → `Записать()`+`Провести()` (контроль свободного остатка
   и FIFO — в проведении). Если `ОплатитьСразу` — создать `ПриходныйКассовыйОрдер` (Касса,
   Контрагент=Покупатель, ДокументРеализации=реализация, Сумма=Реализация.Сумма) → провести.

**Вау:** подбор показывает только реально продаваемое (свободно = остаток − чужие резервы),
цены из прайса, продажа+оплата в один клик.

---

### 3.2 ⭐ АРМ Закупок (планирование потребности) — ФЛАГМАН (аналог дефицита «Кондитерки»)
**Кто:** закупщик. **Цель:** рассчитать дефицит (резервы покупателей − остаток − товары в
пути) и создать заказы поставщикам.

**Документ-АРМ `ПланЗакупок`** (posting:false): шапка — Организация, Склад, Поставщик. ТЧ
**Потребность**: Номенклатура, Заказано(резерв), Остаток, ВПути, Дефицит, КЗаказу.

**Форма — кнопки:**
1. **1️⃣ Рассчитать потребность** — три запроса остатков по складу, свернуть в `Соответствие`
   по номенклатуре:
   - потребность = `РезервыТоваров.Остатки` (ГДЕ Склад; **в SELECT не брать ЗаказПокупателя**
     — автоджойн платформы ищет наименование у документа; брать `Номенклатура, КоличествоОстаток`);
   - остаток = `ОстаткиТоваров.Остатки`; в пути = `ТоварыВПути.Остатки`;
   - `Дефицит = Заказано − Остаток − ВПути` (если <0 → 0); `КЗаказу = Дефицит`. Заполнить ТЧ.
2. **2️⃣ Создать заказ поставщику** — по строкам с `КЗаказу > 0` создать `ЗаказПоставщику`
   (Поставщик из шапки, Склад; строки Номенклатура+Количество=КЗаказу) → `Записать()`+
   `Провести()` (пишет ТоварыВПути).

**Вау:** дефицит с учётом резервов И товаров в пути (не закажешь лишнего); подсветка
дефицита оранжевым (колонка с «дефицит» в имени).

---

### 3.3 АРМ Приёмки товара
**Кто:** кладовщик. **Цель:** принять товар по заказу поставщику, сверить, оформить приход.

**Документ-АРМ `ПриёмкаТовара`** (posting:false): шапка — Организация, Поставщик, Склад,
ЗаказПоставщику. ТЧ **Товары**: Номенклатура, Заказано, Принято, Цена, Расхождение.

**Форма — кнопки:**
1. **1️⃣ Заполнить по заказу** — из `ТоварыВПути.Остатки` ГДЕ ЗаказПоставщику = &Заказ
   (или `Документы.ЗаказПоставщику.…ПолучитьОбъект().Товары`) → строки (Заказано, Принято=
   Заказано, Цена из заказа); `Расхождение = Принято − Заказано`.
2. **2️⃣ Создать поступление** — `ПоступлениеТоваров` (Поставщик, Склад, ЗаказПоставщику;
   строки Номенклатура+Количество=Принято+Цена) → `Записать()`+`Провести()` (закрывает товары
   в пути, создаёт партии FIFO, наш долг поставщику в взаиморасчётах).

**Вау:** приёмка по заказу с подсветкой недопоставки (Расхождение < 0 — красным).

---

### 3.4 АРМ Отгрузки по заказам
**Кто:** менеджер/кладовщик. **Цель:** отгрузить зарезервированное по заказам покупателей.

**Документ-АРМ `ОтгрузкаЗаказов`** (posting:false): шапка — Организация, Покупатель, Склад.
ТЧ **Строки**: ЗаказПокупателя, Номенклатура, Резерв, Остаток, КОтгрузке.

**Форма — кнопки:**
1. **1️⃣ Заполнить к отгрузке** — заказы покупателя: перебрать проведённые `ЗаказПокупателя`
   этого покупателя, по каждому `…ПолучитьОбъект().Товары`; остаток = `ОстаткиТоваров.Остатки`;
   `КОтгрузке = Мин(Резерв, Остаток)`.
2. **2️⃣ Создать реализации** — сгруппировать по заказу, по каждому создать `РеализацияТоваров`
   с `ЗаказПокупателя = заказ` (проведение **снимает резерв** и спишет FIFO) → провести.
   Каждый заказ в `Попытка/Исключение` — сбой одного не рушит пакет.

**Вау:** отгрузка по заказу автоматически снимает резерв; контроль свободного остатка.

---

### 3.5 АРМ Кассира / Оплаты (взаиморасчёты)
**Кто:** кассир/бухгалтер. **Цель:** видеть долги покупателей и наши долги поставщикам,
принимать/выдавать оплату.

**Документ-АРМ `РабочееМестоКассира`** (posting:false): шапка — Организация, Касса. ТЧ
**Долги**: Контрагент, ТипКонтрагента, Долг, КОплате.

**Форма — кнопки:**
1. **1️⃣ Рассчитать долги** — `Взаиморасчеты.Остатки` ГДЕ Организация, свернуть по контрагенту;
   знак/направление по атрибуту `ТипКонтрагента` (Покупатель: долг нам; Поставщик: наш долг).
   `КОплате = |Долг|`.
2. **2️⃣ Принять оплату (ПКО)** — по покупателям с долгом создать `ПриходныйКассовыйОрдер`
   (Касса, Контрагент, Сумма=КОплате) → провести.
3. **3️⃣ Оплатить поставщику (РКО)** — по поставщикам создать `РасходныйКассовыйОрдер`.

**Контроль:** при оплате по конкретному документу — `НепогашенныйДолгОснования(...)` не даёт
переплатить (ядро уже умеет через `ПроверитьНеПереплату`).

**Знак взаиморасчётов (проверено по проведениям):** реализация → `Приход`, `ТипКонтрагента =
"Покупатель"`; поступление → `Расход`, `ТипКонтрагента = "Поставщик"`. Значит по контрагенту
`СуммаОстаток > 0` = покупатель должен нам (→ ПКО), `< 0` = мы должны поставщику (→ РКО).
Группировать строго по `ТипКонтрагента`. Путь поставщика зависит от фикса блокера **A** (§0).

**Вау:** вся дебиторка/кредиторка в одном окне, оплата одним кликом.

---

### 3.6 АРМ Инвентаризации склада
**Кто:** кладовщик. **Цель:** пересчёт, оформление излишков и недостач.

**Документ-АРМ `ИнвентаризацияСклада`** (posting:false): шапка — Организация, Склад. ТЧ
**Ведомость**: Номенклатура, Учёт, Факт, Отклонение.

**Форма — кнопки:**
1. **1️⃣ Заполнить учётные остатки** — `ОстаткиТоваров.Остатки` ГДЕ Склад (с `СУММА` и
   `СГРУППИРОВАТЬ ПО Номенклатура` — на случай нескольких строк) → Учёт; Факт=Учёт.
2. **2️⃣ Оформить излишки и недостачи** — по `Отклонение = Факт − Учёт`:
   - где `>0` → `ОприходованиеТоваров` (Склад; Номенклатура+Количество=Отклонение, Цена=средняя
     `Сумма/Количество` из остатка) → провести;
   - где `<0` → `СписаниеТоваров` (Склад; Номенклатура+Количество=|Отклонение|) → провести
     (себестоимость спишет FIFO).

**Вау:** одна ведомость → сразу два документа; недостача красным, излишек — обычным.

---

## 4. Приоритет реализации
1. **Демо-данные** (раздел 6) — без них АРМы пустые.
2. **АРМ Продавца** (3.1) — самый «вау» для торговли (подбор + свободный остаток + продажа+оплата).
3. **АРМ Закупок** (3.2) — проверенный паттерн дефицита, наглядная подсветка.
4. **АРМ Приёмки** (3.3) и **АРМ Кассы** (3.5) — замыкают цикл деньги↔товар.
5. **АРМ Отгрузки** (3.4) и **АРМ Инвентаризации** (3.6) — тиражирование механики.

Подсистема **«Рабочие места»** (`subsystems/рабочие_места.yaml`) + виджет
`widgets/действия_арм.yaml` (type: actions) со ссылками на все 6 АРМов.

**Роли уже есть** — АРМы ложатся на них 1:1 (добавить право `read, write` на документ-АРМ
соответствующей роли):

| АРМ | Роль (`roles/`) |
|---|---|
| Продавца (3.1), Отгрузки (3.4) | `менеджер` |
| Закупок (3.2) | `снабженец` |
| Приёмки (3.3), Отгрузки, Инвентаризации (3.6) | `кладовщик` |
| Кассира (3.5) | `кассир` |

**Дашборды АРМов собирать из существующих ~50 виджетов** (новые не плодить), напр. в
`home_page` подсистемы «Рабочие места»: `товары_к_заказу`, `ожидаемые_поставки`,
`зарезервировано` (закупки/склад); `дебиторская_задолженность`, `задолженность_поставщикам`,
`касса_остаток`, `денежный_поток` (касса); `остатки_на_складах`, `неликвиды_список`,
`стоимость_склада` (склад).

---

## 5. Тестовые примеры (числовые, проверяются по регистрам)

Гонять headless: `onebase procrun … --proc <Проверка>` или вручную в UI; сверять регистры
через отчёты/служебные страницы регистров накопления.

### Т1 — Продавец (продажа + оплата)
Дано: на «Главном складе» Табурет «Шаткий-2000» остаток 30, чужой резерв 0 → свободно 30,
розничная цена 1 500. Действие: подбор Табурет ×10 → «Оформить продажу» с оплатой.
Ожидаемо: Реализация на 15 000 проведена; `ОстаткиТоваров` Табурет = 20; `ВаловаяПрибыль`
выручка 15000 − себестоимость(FIFO); `Взаиморасчеты` покупателя +15000, затем ПКО −15000 = 0;
`ДенежныеСредства` кассы +15000.

### Т2 — Закупки (дефицит с учётом «в пути»)
Дано: заказ покупателя Стол «Внезапно Зашатался» 50 (резерв 50), остаток 30, в пути 5.
Действие: «Рассчитать потребность». Ожидаемо: Дефицит = 50 − 30 − 5 = **15** (оранжевым).
«Создать заказ поставщику» → ЗаказПоставщику на 15 проведён; `ТоварыВПути` += 15 (итог 20).

### Т3 — Приёмка (закрытие «в пути»)
Дано: ЗаказПоставщику «Дерево и Гвозди» на Стол 10, в пути 10, поступления нет. Действие:
«Заполнить по заказу» (Принято=10) → «Создать поступление». Ожидаемо: `ТоварыВПути` по
заказу = 0; `ОстаткиТоваров`/`ПартииТоваров` += 10; `Взаиморасчеты` поставщика на сумму.
Тест недопоставки: Принято=8 → Расхождение −2 (красным), в пути по заказу остаётся 2.

### Т4 — Касса (гашение долга, контроль переплаты)
Дано: реализация покупателю «Сядь-Полежи» на 40 000 без оплаты → долг 40 000. Действие:
«Рассчитать долги» (видим 40 000) → «Принять оплату (ПКО)». Ожидаемо: ПКО 40 000; долг = 0;
касса += 40 000. Повтор ПКО по той же реализации → исключение «уже полностью оплачена».

### Т5 — Инвентаризация (излишек/недостача)
Дано: на складе Кресло «Я тут главный» учёт 12. Действие: «Заполнить» → Факт 10 →
«Оформить». Ожидаемо: Списание на 2 (Отклонение −2 красным); остаток = 10. Обратный кейс:
Факт 15 → Оприходование на 3; остаток = 15.

### Т6 — Отгрузка (снятие резерва)
Дано: заказ покупателя «Три Стула» на Диван 5 (резерв 5), остаток 8. Действие: «Заполнить»
(КОтгрузке=5) → «Создать реализации». Ожидаемо: Реализация по заказу проведена; `РезервыТоваров`
по заказу = 0; `ОстаткиТоваров` Диван = 3; свободный остаток восстановлен.

---

## 6. Демо-данные (точечные добавления в `заполнитьтестовуюбазу.proc.os`)

Демо уже богатое («мебельный абсурд»: ООО «Табуретка-Люкс»; склады «Где-то там» и уценёнки;
поставщики «Дерево и Гвозди», «Фанера-Палас» и др.; покупатели «Три Стула», «Сядь-Полежи»,
«Гостиница Три подушки», «Ресторан Стол на двоих», «Ремонт-Уют», «Маленький стульчик»;
номенклатура — Табурет «Шаткий-2000», Стол «Внезапно Зашатался», Диван «Проваленный…»,
Кресло «Я тут главный», Кровать «Сон нации», Комод, Полка «Пизанская», Набор «Столовый»).

Добавить целевые ситуации (идемпотентно, по образцу существующих блоков):

1. **Дефицит для АРМ Закупок** — заказ покупателя, который резервирует больше, чем есть+в пути:
   напр. «Гостиница Три подушки» на Стол «Внезапно Зашатался» 50 при остатке ~30 → дефицит.
2. **Открытый заказ поставщику для АРМ Приёмки** — ЗаказПоставщику «Дерево и Гвозди» на Стол
   10, проведён (в пути), без поступления.
3. **Непогашенный долг для АРМ Кассы** — оставить хотя бы одну реализацию без ПКО (долг
   покупателя) и одно поступление без РКО (наш долг поставщику).
4. **Свободный остаток для АРМ Продавца** — гарантировать на «Главном складе» позиции с
   остатком > резервов (Табурет, Кресло, Диван).
5. **Расхождение для АРМ Инвентаризации** — не требует данных (факт вводит пользователь);
   достаточно ненулевого остатка на складе.
6. *(Опционально для контраста)* пустой по части позиций **Склад уценёнки** — показать нулевой
   свободный остаток в подборе Продавца.

В финальном `Сообщить` демо обновить счётчики. Проверить идемпотентность (`НайтиПоНаименованию`
перед `Создать`, как в текущем коде).

---

## 7. Движок и грабли (чек-лист, выверено «Кондитеркой» + специфика PuT)

- **Документ-АРМ:** `posting: false`; форма в `forms/<имя-в-нижнем-регистре>/формаобъекта.form.yaml`
  (+ `.form.os`); плоское размещение не подхватывается.
- **SlickGrid** для ТЧ — по умолчанию; отключение `no_grid: true`. Подсветка чисел уже в движке:
  отрицательные — красным, колонка с «дефицит» в имени при >0 — оранжевым (называть колонку
  именно `Дефицит`/`Отклонение`/`Расхождение`).
- **Подбор:** кнопка с событиями `Нажатие` (`ПоказатьПодбор(Данные, Колонки, Конфиг)`) и `Выбор`
  (`ПодборРезультат`). Данные — Массив структур с полем `Идентификатор` (UUID ссылки).
- **Reference из UUID:** значения измерений из запроса остатков приходят как UUID; класть их в
  reference-поле ТЧ можно напрямую — после round-trip формы платформа покажет наименование, при
  создании документа UUID нормализуется.
- **Свёртка остатков:** при развороте по номенклатуре использовать `СУММА(КоличествоОстаток)` +
  `СГРУППИРОВАТЬ ПО Номенклатура` (несколько строк партий/субконто иначе дадут дубли).
- **РезервыТоваров:** в SELECT **не** указывать измерение `ЗаказПокупателя` (автоджойн ищет
  наименование у документа) — фильтровать только во `ГДЕ`, в SELECT брать `Номенклатура,
  КоличествоОстаток` (см. `ЧужиеРезервыПоСкладу` в утилитах).
- **Виртуальные таблицы — только со скобками:** `…Остатки(&МоментВремени)`; для остатков на
  «сейчас» передавать `ТекущаяДата()`.
- **Чтение ТЧ другого документа:** `Документы.X.НайтиПоНомеру(ном).ПолучитьОбъект().Товары`
  (прямой `Документ.X.Товары` в запросе не работает).
- **Создание документа из формы-АРМ:** заполнять только шапку + (Номенклатура, Количество);
  цены/НДС/суммы досчитает `ПриЗаписи`. Пакет оборачивать в `Попытка/Исключение`.
- **Зарезервированные слова:** `Перем`, одиночное `И` — не использовать как имена переменных.
- **Утилиты не дублировать:** звать `СвободныеОстаткиСклада`, `ПолучитьЦену`,
  `НепогашенныйДолгОснования`, `СуммаНДСстроки` из `утилиты.module.os`.
- **Сборка/перезапуск:** запущенный сервер держит `onebase.exe` и `*.db` — перед пересборкой/
  пересозданием БД останавливать (`taskkill /IM onebase.exe /F`).

---

## 8. Файловая структура и порядок работ

Для каждого АРМа:
```
documents/<имя>.yaml                                  # posting:false, шапка + рабочие ТЧ
forms/<имя-в-нижнем-регистре>/формаобъекта.form.yaml  # шапка, ТЧ (SlickGrid), кнопки
forms/<имя-в-нижнем-регистре>/формаобъекта.form.os    # логика кнопок (зовёт утилиты)
```
Общее:
```
subsystems/рабочие_места.yaml     # подсистема «Рабочие места» + home_page из готовых виджетов
widgets/действия_арм.yaml         # type: actions — ссылки на 6 АРМов (единственный новый виджет)
roles/{менеджер,снабженец,кладовщик,кассир}.yaml  # + read/write на соответствующие документы-АРМ
src/заполнитьтестовуюбазу.proc.os # точечные добавления (раздел 6)
```

**Порядок:** (0) фикс блокера §0.A в `поступлениетоваров.posting.os` → `check` чистый →
(1) демо-добавления → (2) АРМ Продавца → `onebase check` → headless-тест Т1 → (3) АРМ Закупок
→ Т2 → (4) Приёмка/Касса → Т3,Т4 → (5) Отгрузка/Инвентаризация → Т5,Т6 → (6) подсистема +
виджет + роли → финальный `check` и проход по UI.

**Проверка после каждой правки:** `onebase check --project C:\Projects\OneBaseConfs\PuT`
(компилирует и исполняет запросы — ловит ошибки колонок/таблиц до рантайма).
