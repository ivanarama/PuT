# План: Подбор товаров по остаткам (issue #27)

## Контекст

В 1С у расходных документов есть кнопка **«Подбор»**: открывает список номенклатуры
с остатком на складе документа, пользователь отмечает позиции и количества — строки
попадают в табличную часть. Это **епархия конфигурации**, а не платформы: логика
«что считать остатком» (резервы, партии, склад) живёт в прикладном коде.

Эталонный документ — **РеализацияТоваров** (`documents/реализация_товаров.yaml`,
форма `forms/реализациятоваров/`). После отладки — тиражируем на остальные расходные.

## Модель свободного остатка (Склад + минус резервы)

Уже реализована в `src/утилиты.module.os` → `ПроверитьСвободныйОстаток` (строка 170).
Подбор использует **ту же** формулу, чтобы предлагать ровно то, что потом пройдёт
контроль при проведении:

```
Свободно(Ном) = ОстатокПартий(Ном, Склад, Организация) − ЧужиеРезервы(Ном, Склад)
```

- **Остаток** — `РегистрНакопления.ПартииТоваров.Остатки(&МоментВремени)` по
  `Склад` + `Организация`, агрегат `КоличествоОстаток > 0` (тот же источник, что и ФИФО).
- **Чужие резервы** — `РегистрНакопления.РезервыТоваров.Остатки(&МоментВремени)` по
  `Склад`, исключая `ЗаказПокупателя = СвойЗаказ` документа (свой резерв подбору не мешает).
- `МоментВремени` — для нового/непроведённого документа берём `ТекущаяДата()`
  (у объекта ещё нет `МоментВремени()`); для записанного — `Объект.Ссылка.МоментВремени()`.

> Регистр `ОстаткиТоваров` тоже годится как источник остатка (он без партий и проще),
> но для согласованности с проверкой при проведении берём `ПартииТоваров` — иначе подбор
> мог бы предложить позицию, которую ФИФО потом не найдёт в нужной партии.

## Объём итерации

Делаем **этап 1** (чистая конфигурация, отгружаем сейчас). **Этап 2** —
по согласованию, требует доработки движка.

---

## Этап 1 — «Заполнить по остаткам» (чистая конфигурация)

Кнопка на вкладке «Товары» формы реализации, серверный обработчик заполняет ТЧ
кандидатами: все номенклатуры со свободным остатком > 0 на складе документа.
Количество по умолчанию = свободный остаток (пользователь правит/удаляет лишнее),
цена подтягивается через существующую `ПолучитьЦену`.

Механизм уже поддержан движком: управляемая форма исполняет `obFire('Имя','Нажатие')`
→ серверный обработчик в `.form.os` → ре-рендер формы с новыми строками
(см. `КнопкаОчиститьНажатие`, который вызывает `Объект.Товары.Очистить()`).

### Изменения

**1. Общая функция подбора — `src/утилиты.module.os`** (рядом с `ПроверитьСвободныйОстаток`):

```
// Возвращает Массив структур {Номенклатура, Свободно} со свободным остатком > 0
// на складе. СвойЗаказ — заказ текущего документа (его резерв не вычитается),
// Неопределено если заказа нет.
Функция СвободныеОстаткиСклада(Склад, Организация, СвойЗаказ, МоментВремени) Экспорт
```

Тело — переиспользует запросы из `ПроверитьСвободныйОстаток` без списка номенклатуры
(берём всё по складу), возвращает результат вместо `ВызватьИсключение`. Чтобы не
дублировать SQL — вынести два запроса (резервы, партии) в локальные хелперы и звать
из обеих процедур.

**2. Кнопка в форме — `forms/реализациятоваров/формаобъекта.form.yaml`**
(вкладка `СтраницаТовары`, рядом с `КнопкаОчистить`):

```yaml
- kind: Кнопка
  name: КнопкаПодбор
  title:
    ru: "Заполнить по остаткам"
    en: "Fill from balances"
    de: "Aus Beständen füllen"
    sr: "Попуни из стања"
  hint: "Добавить товары со свободным остатком на складе документа"
  events:
    Нажатие: КнопкаПодборНажатие
```

**3. Обработчик — `forms/реализациятоваров/формаобъекта.form.os`** (`&НаСервере`):

```
Процедура КнопкаПодборНажатие()
    Если НЕ ЗначениеЗаполнено(Объект.Склад) Тогда
        ВызватьИсключение("Сначала укажите склад отгрузки");
    КонецЕсли;
    Орг = ?(ЗначениеЗаполнено(Объект.Организация), Объект.Организация, ОрганизацияПоУмолчанию());
    Момент = ?(ЗначениеЗаполнено(Объект.Дата), Объект.Дата, ТекущаяДата());

    // уже добавленные позиции — чтобы не дублировать
    Есть = Новый Соответствие;
    Для Каждого Стр Из Объект.Товары Цикл
        Есть.Вставить(Стр.Номенклатура, Истина);
    КонецЦикла;

    Кандидаты = СвободныеОстаткиСклада(Объект.Склад, Орг, Объект.ЗаказПокупателя, Момент);
    Добавлено = 0;
    Для Каждого К Из Кандидаты Цикл
        Если Есть.Получить(К.Номенклатура) <> Неопределено Тогда Продолжить; КонецЕсли;
        Нов = Объект.Товары.Добавить();
        Нов.Номенклатура = К.Номенклатура;
        Нов.Количество   = К.Свободно;
        Если ЗначениеЗаполнено(Объект.ТипЦенПродажи) Тогда
            Нов.Цена = ПолучитьЦену(К.Номенклатура, Объект.ТипЦенПродажи, Объект.Дата);
        КонецЕсли;
        Добавлено = Добавлено + 1;
    КонецЦикла;

    ПересчитатьИтоги();
    Сообщить(?(Добавлено = 0, "Свободных остатков на складе нет",
        "Добавлено позиций: " + Строка(Добавлено)));
КонецПроцедуры
```

### Проверка этапа 1

- `onebase check` — зелёный (синтаксис формы + компиляция запросов).
- Тестовая база (`заполнитьтестовуюбазу`): открыть новую РеализациюТоваров, указать
  склад → «Заполнить по остаткам» → строки появились, количества = свободный остаток.
- Зарезервированный под чужой заказ товар **не** предлагается сверх свободного.
- Провести документ — контроль `ПроверитьСвободныйОстаток` не должен ругаться на
  автозаполненные строки (формулы согласованы).

---

## Этап 2 — интерактивный диалог подбора ✅ движок готов (план 46)

Полноценный 1С-подобный подбор: модальный список номенклатуры с колонкой «Свободно»,
чекбоксы + ввод количества, поиск/фильтр, кнопка «Перенести в документ». Возвращает
**выбранные** строки, а не все.

**Платформа уже поддерживает** (репозиторий `onebase`, план 46 — реализовано):

- Билтин **`ПоказатьПодбор(Данные, Колонки, Конфиг)`** — открывает модальный диалог
  мультивыбора. Терминальный: вызывать последним в обработчике фазы 1.
- Событие формы **`Выбор`** + переменная **`ПодборРезультат`** (Массив строк диалога)
  во второй фазе.
- Команды табличной части: кнопки-команды в командной панели над ТЧ + переменная
  `ВыделенныеСтроки` (для команд «по выделенным», подбору не нужна).

Двухфазная схема: кнопка `Нажатие` собирает кандидатов и зовёт `ПоказатьПодбор`
(фаза 1) → пользователь отмечает строки и жмёт «Перенести» → событие `Выбор`
получает `ПодборРезультат` и добавляет строки в ТЧ (фаза 2).

### Изменения конфигурации

**1. Форма — `forms/реализациятоваров/формаобъекта.form.yaml`.**
У кнопки `КнопкаПодбор` добавить событие `Выбор` и сменить заголовок на «Подбор»;
по желанию — перенести командные кнопки ВНУТРЬ элемента `ТабличнаяЧасть ТабТовары`
(`children`), тогда движок отрисует их командной панелью над таблицей (план 46, A1):

```yaml
- kind: ТабличнаяЧасть
  name: ТабТовары
  data_path: Объект.Товары
  children:
    - kind: Кнопка
      name: КнопкаПодбор
      title: { ru: "Подбор", en: "Pick items", de: "Auswahl", sr: "Избор" }
      hint: "Подобрать товары со свободным остатком на складе"
      events:
        Нажатие: КнопкаПодборНажатие   # фаза 1 — показать диалог
        Выбор:   КнопкаПодборВыбор     # фаза 2 — перенести выбранное
    - kind: Кнопка
      name: КнопкаПересчитать
      title: { ru: "Пересчитать суммы" }
      events: { Нажатие: КнопкаПересчитатьНажатие }
    - kind: Кнопка
      name: КнопкаОчистить
      title: { ru: "Очистить таблицу" }
      events: { Нажатие: КнопкаОчиститьНажатие }
```

> Если оставить кнопки соседями ТЧ (как сейчас) — тоже работает, просто без панели
> над таблицей. Командная панель — это UX-сахар, для подбора не обязателен.

**2. Обработчики — `forms/реализациятоваров/формаобъекта.form.os`.**
Заменить текущий «автозаполняющий» `КнопкаПодборНажатие` на фазу 1 (показ диалога)
и добавить фазу 2. `СвободныеОстаткиСклада` из `src/утилиты.module.os` —
переиспользуется без изменений.

```
// @directive=&НаСервере
// ФАЗА 1 — собрать кандидатов и открыть диалог подбора.
Процедура КнопкаПодборНажатие()
    Если НЕ ЗначениеЗаполнено(Объект.Склад) Тогда
        ВызватьИсключение("Сначала укажите склад отгрузки");
    КонецЕсли;
    Орг = ?(ЗначениеЗаполнено(Объект.Организация), Объект.Организация, ОрганизацияПоУмолчанию());
    Момент = ?(ЗначениеЗаполнено(Объект.Дата), Объект.Дата, ТекущаяДата());

    // Уже добавленные позиции исключаем из предложения (по наименованию).
    Есть = Новый Соответствие;
    Для Каждого Стр Из Объект.Товары Цикл
        Есть.Вставить(Строка(Стр.Номенклатура), Истина);
    КонецЦикла;

    Кандидаты = СвободныеОстаткиСклада(Объект.Склад, Орг, Объект.ЗаказПокупателя, Момент);

    // Описание колонок диалога (массив структур).
    Колонки = Новый Массив;
    Колонки.Добавить(Новый Структура("Имя,Заголовок,Тип,Редактируемое", "Номенклатура", "Номенклатура", "string", Ложь));
    Колонки.Добавить(Новый Структура("Имя,Заголовок,Тип,Редактируемое", "Свободно",     "Свободно",     "number", Ложь));
    Колонки.Добавить(Новый Структура("Имя,Заголовок,Тип,Редактируемое", "Количество",   "Кол-во",       "number", Истина));

    // Строки диалога. Идентификатор = наименование (фаза 2 ищет НайтиПоНаименованию).
    Данные = Новый Массив;
    Для Каждого К Из Кандидаты Цикл
        Имя = Строка(К.Номенклатура);
        Если Есть.Получить(Имя) <> Неопределено Тогда Продолжить; КонецЕсли;
        Данные.Добавить(Новый Структура(
            "Идентификатор,Номенклатура,Свободно,Количество",
            Имя, Имя, К.Свободно, К.Свободно));
    КонецЦикла;

    Если Данные.Количество() = 0 Тогда
        Сообщить("Свободных остатков на складе нет");
        Возврат;
    КонецЕсли;

    Конфиг = Новый Структура("Заголовок,ПолеПоиска,ПолеКоличества,ВыбратьВсе",
        "Подбор по остаткам", "Номенклатура", "Количество", Ложь);

    ПоказатьПодбор(Данные, Колонки, Конфиг);   // терминально — открывает диалог
КонецПроцедуры

// @directive=&НаСервере
// ФАЗА 2 — перенести отмеченные строки диалога в табличную часть.
Процедура КнопкаПодборВыбор()
    Добавлено = 0;
    Для Каждого Стр Из ПодборРезультат Цикл
        Кол = Число(Стр.Количество);
        Если Кол <= 0 Тогда Продолжить; КонецЕсли;
        НомСсылка = Справочники.Номенклатура.НайтиПоНаименованию(Стр.Номенклатура);
        Если ЗначениеЗаполнено(НомСсылка) = Ложь Тогда Продолжить; КонецЕсли;
        Нов = Объект.Товары.Добавить();
        Нов.Номенклатура = НомСсылка;
        Нов.Количество   = Кол;
        Если ЗначениеЗаполнено(Объект.ТипЦенПродажи) Тогда
            Нов.Цена = ПолучитьЦену(НомСсылка, Объект.ТипЦенПродажи, Объект.Дата);
        КонецЕсли;
        Добавлено = Добавлено + 1;
    КонецЦикла;
    ПересчитатьИтоги();
    Сообщить("Подобрано позиций: " + Строка(Добавлено));
КонецПроцедуры
```

### Контракт билтина (шпаргалка)

- `ПоказатьПодбор(Данные, Колонки[, Конфиг])`.
- **Данные** — Массив структур; поле `Идентификатор` (или `ID`) → `Стр.id`/`row.ID`
  в результате; остальные поля читаются по `Имя` колонок.
- **Колонки** — Массив структур `{Имя, Заголовок, Тип(string|number|reference),
  Редактируемое(Истина/Ложь)}`. Редактируемые числовые → `<input number>` (количество).
- **Конфиг** — Структура `{Заголовок, ПолеПоиска, ПолеКоличества, ВыбратьВсе}`.
- **ПодборРезультат** (фаза 2) — Массив строк; у каждой `Стр.<ИмяКолонки>` и `Стр.id`.
  Значения редактируемых полей приходят строкой — оборачивать `Число(...)`.
- Для показа в диалоге кладите в данные **наименования/строки**, а не ссылки
  (ссылка сериализуется как UUID и будет нечитаемой). Идентификатор для обратного
  поиска — наименование (`НайтиПоНаименованию`) либо UUID (если используете
  идентификатор ссылки и поиск по нему).

### Проверка этапа 2

- `onebase check` — зелёный.
- Новая РеализацияТоваров → склад → «Подбор» → открылся диалог со списком и остатками.
- Отметить 2–3 позиции, изменить количество → «Перенести» → строки в ТЧ, цена
  подтянулась, итоги пересчитаны.
- Уже добавленная номенклатура не предлагается повторно.
- «Отмена» в диалоге — ТЧ без изменений.

---

## Тиражирование (после отладки на реализации)

Те же кнопка + обработчик (через общую `СвободныеОстаткиСклада`) для расходных:
`ПеремещениеТоваров`, `СписаниеТоваров`, `ВозвратПоставщику`. Для `ЗаказПокупателя`
подбор по остаткам тоже уместен (резервирование), но там «свободно» считается без
вычитания собственного будущего резерва — вынести параметром.
