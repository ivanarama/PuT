# План: Добавление проверок проведения документов PuT

## Контекст

**Проблема:** В конфигурации PuT отсутствует ряд критических проверок при проведении документов. В частности, можно провести реализацию с нулевой суммой в строках (если цена = 0 и не указан тип цен), что создаёт нулевые движения в регистрах Взаиморасчеты и ВаловаяПрибыль — тихо искажая бухгалтерию. Аналогичные пробелы существуют в большинстве документов.

**Причина:** Проверки `ПроверитьЗаполнениеТоваров()` контролируют только непустоту ТЧ и Количество > 0, но не проверяют Номенклатура заполнена, Сумма > 0, реквизиты шапки (Склад, Контрагент). Денежные документы не вызывают готовую `ПроверитьЗаполнениеДенежногоДокумента()`. УстановкаЦен вообще не валидирует ТЧ при проведении.

**Результат:** Проведение любого документа с некорректными данными (пустая номенклатура, нулевая цена, незаполненный склад/контрагент) будет отклонено с понятным сообщением об ошибке.

---

## Шаг 1: Новые процедуры в `src/утилиты.module.os`

### 1A. `ПроверитьЗаполнениеСтрокТоваров` — расширенная проверка строк ТЧ

Оборачивает существующую `ПроверитьЗаполнениеТоваров` и добавляет:
- Номенклатура заполнена в каждой строке
- Сумма > 0 (опционально — параметр `ПроверятьСумму`, по умолчанию Истина)

Параметр `ПроверятьСумму = Ложь` для документов без цены (Перемещение, Сборка, Разборка) и документов где нулевая сумма допустима (Оприходование — бесплатные излишки, Заказы — цена ещё не согласована).

### 1B. `ПроверитьРеквизитШапки` — проверка заполненности реквизита

Хелпер: `ЗначениеЗаполнено(Значение) = Ложь → ВызватьИсключение("Не заполнен реквизит шапки: " + Имя)`

Избегает дублирования кода `ЗначениеЗаполнено` + `ВызватьИсключение` в каждом posting-файле.

---

## Шаг 2: Исправление posting-файлов (16 документов)

Во всех файлах — замена/добавление проверок **в начале `ОбработкаПроведения`**, после `ПроверитьДатуЗапрета`.

### Торговые документы (с ценами)

| Документ | Файл | Заменить `ПроверитьЗаполнениеТоваров` → `ПроверитьЗаполнениеСтрокТоваров` | Добавить `ПроверитьРеквизитШапки` |
|---|---|---|---|
| РеализацияТоваров | `реализациятоваров.posting.os` | ✅ (Сумма > 0) | Покупатель, Склад |
| ПоступлениеТоваров | `поступлениетоваров.posting.os` | ✅ (Сумма > 0) | Поставщик, Склад |
| ВозвратОтПокупателя | `возвратотпокупателя.posting.os` | ✅ (Сумма > 0) | Покупатель, Склад |
| ВозвратПоставщику | `возвратпоставщику.posting.os` | ✅ (Сумма > 0) | Поставщик, Склад |
| ЗаказПокупателя | `заказпокупателя.posting.os` | ✅ (ПроверятьСумму=Ложь) | Покупатель, Склад |
| ЗаказПоставщику | `заказпоставщику.posting.os` | ✅ (ПроверятьСумму=Ложь) | Поставщик, Склад |

### Складские документы (без цен)

| Документ | Файл | Заменить | Добавить `ПроверитьРеквизитШапки` |
|---|---|---|---|
| ПеремещениеТоваров | `перемещениетоваров.posting.os` | ✅ (ПроверятьСумму=Ложь) | СкладОтправитель, СкладПолучатель + совпадение складов |
| ОприходованиеТоваров | `оприходованиетоваров.posting.os` | ✅ (ПроверятьСумму=Ложь) | Склад |
| СписаниеТоваров | `списаниетоваров.posting.os` | ✅ (ПроверятьСумму=Ложь) | Склад |
| СборкаТоваров | `сборкатоваров.posting.os` | ✅ обе ТЧ (ПроверятьСумму=Ложь) | Склад |
| РазборкаТоваров | `разборкатоваров.posting.os` | ✅ обе ТЧ (ПроверятьСумму=Ложь) | Склад |

### УстановкаЦен — критический фикс

`установкацен.posting.os` — **нет вообще никаких проверок ТЧ**. Добавить:
- `ПроверитьРеквизитШапки(this.ТипЦен, "Тип цен")`
- `ПроверитьЗаполнениеСтрокТоваров(this.Цены, "Цены", Ложь)` — ТЧ не пустая, Номенклатура заполнена
- Цикл проверки `Строка.Цена > 0` — нулевая цена перезапишет существующие корректные цены

### Денежные документы — добавить вызов существующей утилиты

| Документ | Добавить |
|---|---|
| `приходныйкассовыйордер.posting.os` | `ПроверитьЗаполнениеДенежногоДокумента(this.Сумма, this.Контрагент, this.Касса)` |
| `расходныйкассовыйордер.posting.os` | `ПроверитьЗаполнениеДенежногоДокумента(this.Сумма, this.Контрагент, this.Касса)` |
| `поступлениенарасчётныйсчёт.posting.os` | `ПроверитьЗаполнениеДенежногоДокумента(this.Сумма, this.Контрагент, this.БанковскийСчёт)` |
| `списаниесрасчётногосчёта.posting.os` | `ПроверитьЗаполнениеДенежногоДокумента(this.Сумма, this.Контрагент, this.БанковскийСчёт)` |

---

## Ключевые файлы

- `src/утилиты.module.os` — добавить 2 новые процедуры
- `src/реализациятоваров.posting.os` — шаблон для торговых документов
- `src/установкацен.posting.os` — критический (нет проверок)
- `src/приходныйкассовыйордер.posting.os` — шаблон для денежных документов
- `src/перемещениетоваров.posting.os` — + проверка совпадения складов
- Ещё 11 файлов `.posting.os` — механические замены

---

## Проверка

1. `onebase check` — валидация синтаксиса всех файлов
2. Проверка в тестовой базе: попытка провести документ с нулевой ценой → ошибка с понятным текстом
3. Регрессия: корректные документы (с заполненными данными) проводятся без изменений
