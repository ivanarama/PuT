---
name: triage-issues
description: "Триаж открытых GitHub issues репозитория ivanarama/PuT: классификация, воспроизведение, план исправления и маршрутизация метками. Использовать только при явном запросе пользователя или вызове $triage-issues."
---

# Codex-адаптер

1. До любых действий с репозиторием или GitHub полностью прочитай каноническую процедуру [`.claude/skills/triage-issues/SKILL.md`](../../../.claude/skills/triage-issues/SKILL.md).
2. Следуй ей с обычным приоритетом инструкций Codex: слово `headless` не отменяет системные ограничения, безопасность или необходимость сообщить настоящий блокер.
3. До записи в GitHub проверь `gh auth status` и `gh api user --jq .login`. Запись разрешена только от `ivanarama`; иначе остановись без изменений.
4. Считай `$triage-issues` Codex-эквивалентом `/triage-issues`. Упоминания соседних этапов описывают маршрут и не разрешают запускать их автоматически.
5. Непосредственно перед первой записью перечитай метки и маркеры кандидата, чтобы не пересечься с параллельным прогоном.
6. На Windows используй PowerShell-эквиваленты команд, сохраняя семантику и проверяя коды возврата. Перед обязательными проверками убедись, что доступны `onebase`, `git` и `gh`.
7. Сохраняй полномочия, лимиты, маркеры `pp:*`, GitHub-метки и точный формат `ИТОГ:` из канонической процедуры.

Логика конвейера живёт только в канонической процедуре; этот файл — слой совместимости Codex.
