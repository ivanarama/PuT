---
name: review-queue
description: "Независимое ревью открытых pull requests ivanarama/PuT с проверкой диффа и проектных проверок. Использовать только при явном запросе пользователя или вызове $review-queue."
---

# Codex-адаптер

1. Сначала прочитай короткий общий маршрут [`.claude/skills/review-queue/SKILL.md`](../../../.claude/skills/review-queue/SKILL.md) и используй `pipelinectl` из задачи.
2. Следуй ей с обычным приоритетом инструкций Codex: слово `headless` не отменяет системные ограничения, безопасность или необходимость сообщить настоящий блокер.
3. До записи в GitHub проверь `gh auth status` и `gh api user --jq .login`. Запись разрешена только от `ivanarama`; иначе остановись без изменений.
4. Считай `$review-queue` Codex-эквивалентом `/review-queue`. Упоминания соседних этапов описывают маршрут и не разрешают запускать их автоматически.
5. Сохраняй независимость ревью, breadth-first порядок обычной очереди и
   single-flight-владение интеграционным REVIEW: за запуск проверяется только
   первый доказанный base-sync/legacy re-ship, общий `main` не изменяется.
   `review_candidates` из обязательного `pipelinehealth` preflight — закрытый
   allowlist; не строй запасную очередь. Пока владелец ждёт MERGE/recovery,
   обычные stage `review` остаются разрешёнными. Интеграционная проверка
   переиспользует content-proof и смотрит только base-sync дельту, конфликты и
   CI, а не повторяет весь содержательный аудит.
6. На Windows используй PowerShell-эквиваленты команд, сохраняя семантику и проверяя коды возврата. Перед обязательными проверками убедись, что доступны `onebase`, `git` и `gh`.
7. Полную `references/legacy-protocol.md` читай только при `action=fallback`;
   не загружай её в обычном tool-пути.

Обычная механика живёт в `pipelinectl`, сложные состояния — в fallback.
